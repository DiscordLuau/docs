---
title: Aes256gcm
description: DiscordLuau docs for Aes256gcm.
sidebar:
  order: 100
---

[//]: # (This file was @generated from moonwave comments. Do not edit by hand.)
[//]: # (To edit docs, change the source comments in the DiscordLuau repo.)

import LuaDeclaration from '$/components/LuaDeclaration.astro';
import LuaProperty from '$/components/LuaProperty.astro';
import LuaSummary from '$/components/LuaSummary.astro';
import LuaParamTable from '$/components/LuaParamTable.astro';

AES-256-GCM authenticated encryption, detached tag variant.

Requires AES-NI hardware support - call isAvailable() before use; libsodium will abort on CPUs without it.
Uses a 12-byte nonce; always use a strict counter and rotate the key before reaching 2^32.

<LuaSummary className="Aes256gcm" functions={[ { name: "isAvailable", args: [  ], returnType: "boolean" }, { name: "keygen", args: [  ], returnType: "buffer" }, { name: "encrypt", args: [ { name: "plaintext", type: "buffer" }, { name: "key", type: "buffer" }, { name: "nonce", type: "buffer" }, { name: "ad", type: "buffer?" } ], returnType: "EncryptResult" }, { name: "decrypt", args: [ { name: "cipher", type: "buffer" }, { name: "tag", type: "buffer" }, { name: "key", type: "buffer" }, { name: "nonce", type: "buffer" }, { name: "ad", type: "buffer?" } ], returnType: "buffer" } ]} />

## Functions

### isAvailable

> <LuaDeclaration name="isAvailable" className="Aes256gcm" args={[  ]} returnType="boolean" />

Returns true if the current CPU supports AES-NI hardware acceleration.

<LuaParamTable returns={[ { type: "boolean" } ]} />

### keygen

> <LuaDeclaration name="keygen" className="Aes256gcm" args={[  ]} returnType="buffer" />

Generates a random 32-byte AES-256-GCM key via libsodium's CSPRNG.

<LuaParamTable returns={[ { type: "buffer" } ]} />

### encrypt

> <LuaDeclaration name="encrypt" className="Aes256gcm" args={[ { name: "plaintext", type: "buffer" }, { name: "key", type: "buffer" }, { name: "nonce", type: "buffer" }, { name: "ad", type: "buffer?" } ]} returnType="EncryptResult" />

Encrypts `plaintext` with `key` (32 bytes) and `nonce` (12 bytes).
Returns `{ cipher, tag }` where `cipher` is the same length as `plaintext` and `tag` is 16 bytes.

<LuaParamTable params={[ { name: "plaintext", type: "buffer" }, { name: "key", type: "buffer" }, { name: "nonce", type: "buffer" }, { name: "ad", type: "buffer?" } ]} returns={[ { type: "EncryptResult" } ]} />

### decrypt

> <LuaDeclaration name="decrypt" className="Aes256gcm" args={[ { name: "cipher", type: "buffer" }, { name: "tag", type: "buffer" }, { name: "key", type: "buffer" }, { name: "nonce", type: "buffer" }, { name: "ad", type: "buffer?" } ]} returnType="buffer" />

Decrypts `cipher` using its detached `tag`, `key` (32 bytes), and `nonce` (12 bytes).
Returns the plaintext buffer; errors if authentication tag does not verify.

<LuaParamTable params={[ { name: "cipher", type: "buffer" }, { name: "tag", type: "buffer" }, { name: "key", type: "buffer" }, { name: "nonce", type: "buffer" }, { name: "ad", type: "buffer?" } ]} returns={[ { type: "buffer" } ]} />


