---
title: Secretbox
description: DiscordLuau docs for Secretbox.
sidebar:
  order: 100
---

[//]: # (This file was @generated from moonwave comments. Do not edit by hand.)
[//]: # (To edit docs, change the source comments in the DiscordLuau repo.)

import LuaDeclaration from '$/components/LuaDeclaration.astro';
import LuaProperty from '$/components/LuaProperty.astro';
import LuaSummary from '$/components/LuaSummary.astro';
import LuaParamTable from '$/components/LuaParamTable.astro';

XSalsa20-Poly1305 symmetric authenticated encryption with a shared secret key.
Both combined (`seal`/`open`) and detached (`sealDetached`/`openDetached`) forms are available.
Combined: 16-byte MAC prepended to ciphertext. Detached: MAC and ciphertext returned separately as `{ cipher, mac }`.

<LuaSummary className="Secretbox" functions={[ { name: "keygen", args: [  ], returnType: "buffer" }, { name: "nonce", args: [  ], returnType: "buffer" }, { name: "seal", args: [ { name: "message", type: "buffer" }, { name: "key", type: "buffer" }, { name: "nonce", type: "buffer" } ], returnType: "buffer" }, { name: "open", args: [ { name: "ciphertext", type: "buffer" }, { name: "key", type: "buffer" }, { name: "nonce", type: "buffer" } ], returnType: "buffer" }, { name: "sealDetached", args: [ { name: "message", type: "buffer" }, { name: "key", type: "buffer" }, { name: "nonce", type: "buffer" } ], returnType: "SealResult" }, { name: "openDetached", args: [ { name: "cipher", type: "buffer" }, { name: "mac", type: "buffer" }, { name: "key", type: "buffer" }, { name: "nonce", type: "buffer" } ], returnType: "buffer" } ]} />

## Functions

### keygen

> <LuaDeclaration name="keygen" className="Secretbox" args={[  ]} returnType="buffer" />

Generates a random 32-byte secretbox key via libsodium's CSPRNG.

<LuaParamTable returns={[ { type: "buffer" } ]} />

### nonce

> <LuaDeclaration name="nonce" className="Secretbox" args={[  ]} returnType="buffer" />

Generates a random 24-byte nonce via libsodium's CSPRNG.
Generate a fresh nonce for every message - never reuse a nonce with the same key.

<LuaParamTable returns={[ { type: "buffer" } ]} />

### seal

> <LuaDeclaration name="seal" className="Secretbox" args={[ { name: "message", type: "buffer" }, { name: "key", type: "buffer" }, { name: "nonce", type: "buffer" } ]} returnType="buffer" />

Encrypts `message` with `key` (32 bytes) and `nonce` (24 bytes).
Returns a combined buffer: `[mac (16 B)][ciphertext]`. Errors if encryption fails.

<LuaParamTable params={[ { name: "message", type: "buffer" }, { name: "key", type: "buffer" }, { name: "nonce", type: "buffer" } ]} returns={[ { type: "buffer" } ]} />

### open

> <LuaDeclaration name="open" className="Secretbox" args={[ { name: "ciphertext", type: "buffer" }, { name: "key", type: "buffer" }, { name: "nonce", type: "buffer" } ]} returnType="buffer" />

Decrypts a combined `ciphertext` (produced by `seal`) with `key` and `nonce`.
Returns the plaintext. Errors if the MAC does not verify.

<LuaParamTable params={[ { name: "ciphertext", type: "buffer" }, { name: "key", type: "buffer" }, { name: "nonce", type: "buffer" } ]} returns={[ { type: "buffer" } ]} />

### sealDetached

> <LuaDeclaration name="sealDetached" className="Secretbox" args={[ { name: "message", type: "buffer" }, { name: "key", type: "buffer" }, { name: "nonce", type: "buffer" } ]} returnType="SealResult" />

Encrypts `message` with `key` (32 bytes) and `nonce` (24 bytes), returning MAC and ciphertext separately.
Ciphertext is the same length as the message; result is `{ cipher, mac }`. Errors if encryption fails.

<LuaParamTable params={[ { name: "message", type: "buffer" }, { name: "key", type: "buffer" }, { name: "nonce", type: "buffer" } ]} returns={[ { type: "SealResult" } ]} />

### openDetached

> <LuaDeclaration name="openDetached" className="Secretbox" args={[ { name: "cipher", type: "buffer" }, { name: "mac", type: "buffer" }, { name: "key", type: "buffer" }, { name: "nonce", type: "buffer" } ]} returnType="buffer" />

Decrypts `cipher` using its detached `mac`, `key` (32 bytes), and `nonce` (24 bytes).
Returns the plaintext. Errors if the MAC does not verify.

<LuaParamTable params={[ { name: "cipher", type: "buffer" }, { name: "mac", type: "buffer" }, { name: "key", type: "buffer" }, { name: "nonce", type: "buffer" } ]} returns={[ { type: "buffer" } ]} />


