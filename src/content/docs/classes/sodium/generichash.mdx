---
title: Generichash
description: DiscordLuau docs for Generichash.
sidebar:
  order: 100
---

[//]: # (This file was @generated from moonwave comments. Do not edit by hand.)
[//]: # (To edit docs, change the source comments in the DiscordLuau repo.)

import LuaDeclaration from '$/components/LuaDeclaration.astro';
import LuaProperty from '$/components/LuaProperty.astro';
import LuaSummary from '$/components/LuaSummary.astro';
import LuaParamTable from '$/components/LuaParamTable.astro';

BLAKE2b hashing with variable output length (16–64 bytes), optional key, and streaming support.
Can be used unkeyed (like SHA-256) or keyed (as a MAC).
One-shot (`hash`): hashes a buffer in a single call.
Streaming (`new` → `update` → `final`): hashes data incrementally; `final` may only be called once.

<LuaSummary className="Generichash" functions={[ { name: "hash", args: [ { name: "message", type: "buffer" }, { name: "outputBytes", type: "number?" }, { name: "key", type: "buffer?" } ], returnType: "buffer" } ]} />

## Functions

### hash

> <LuaDeclaration name="hash" className="Generichash" args={[ { name: "message", type: "buffer" }, { name: "outputBytes", type: "number?" }, { name: "key", type: "buffer?" } ]} returnType="buffer" />

Hashes `message` in one call and returns a digest of `outputBytes` bytes (default 32, range 16–64).
Optionally accepts a `key` (16–64 bytes) to produce a keyed BLAKE2b digest.

<LuaParamTable params={[ { name: "message", type: "buffer" }, { name: "outputBytes", type: "number?" }, { name: "key", type: "buffer?" } ]} returns={[ { type: "buffer" } ]} />


