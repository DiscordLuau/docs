---
title: Sign
description: DiscordLuau docs for Sign.
sidebar:
  order: 100
---

[//]: # (This file was @generated from moonwave comments. Do not edit by hand.)
[//]: # (To edit docs, change the source comments in the DiscordLuau repo.)

import LuaDeclaration from '$/components/LuaDeclaration.astro';
import LuaProperty from '$/components/LuaProperty.astro';
import LuaSummary from '$/components/LuaSummary.astro';
import LuaParamTable from '$/components/LuaParamTable.astro';

Ed25519 digital signatures using the detached form - the signature is separate from the message.
Signs with a 64-byte secret key; verifies with the corresponding 32-byte public key.
`verify` returns a boolean rather than erroring, so callers can branch without a `pcall` wrapper.

<LuaSummary className="Sign" functions={[ { name: "keypair", args: [  ], returnType: "Keypair" }, { name: "secretKeyToPublicKey", args: [ { name: "secretKey", type: "buffer" } ], returnType: "buffer" }, { name: "sign", args: [ { name: "message", type: "buffer" }, { name: "secretKey", type: "buffer" } ], returnType: "buffer" }, { name: "verify", args: [ { name: "signature", type: "buffer" }, { name: "message", type: "buffer" }, { name: "publicKey", type: "buffer" } ], returnType: "boolean" } ]} />

## Functions

### keypair

> <LuaDeclaration name="keypair" className="Sign" args={[  ]} returnType="Keypair" />

Generates a random Ed25519 keypair; public key is safe to share, secret key must be kept private.
The secret key is 64 bytes (seed + public key); call `secretKeyToPublicKey` to recover the public key from it.

<LuaParamTable returns={[ { type: "Keypair" } ]} />

### secretKeyToPublicKey

> <LuaDeclaration name="secretKeyToPublicKey" className="Sign" args={[ { name: "secretKey", type: "buffer" } ]} returnType="buffer" />

Derives the 32-byte public key from a 64-byte Ed25519 `secretKey`.
Useful when only the secret key was persisted and the public key needs to be recovered.

<LuaParamTable params={[ { name: "secretKey", type: "buffer" } ]} returns={[ { type: "buffer" } ]} />

### sign

> <LuaDeclaration name="sign" className="Sign" args={[ { name: "message", type: "buffer" }, { name: "secretKey", type: "buffer" } ]} returnType="buffer" />

Signs `message` with `secretKey` (64 bytes) and returns the 64-byte detached signature.
The message itself is not modified or included in the return value.

<LuaParamTable params={[ { name: "message", type: "buffer" }, { name: "secretKey", type: "buffer" } ]} returns={[ { type: "buffer" } ]} />

### verify

> <LuaDeclaration name="verify" className="Sign" args={[ { name: "signature", type: "buffer" }, { name: "message", type: "buffer" }, { name: "publicKey", type: "buffer" } ]} returnType="boolean" />

Verifies `signature` (64 bytes) against `message` under `publicKey` (32 bytes).
Returns `true` if valid, `false` otherwise - does not error on invalid signatures.

<LuaParamTable params={[ { name: "signature", type: "buffer" }, { name: "message", type: "buffer" }, { name: "publicKey", type: "buffer" } ]} returns={[ { type: "boolean" } ]} />


