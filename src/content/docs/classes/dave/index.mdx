---
title: Dave
description: DiscordLuau docs for Dave.
sidebar:
  label: Overview
  order: 0
---

[//]: # (This file was @generated from moonwave comments. Do not edit by hand.)
[//]: # (To edit docs, change the source comments in the DiscordLuau repo.)

import LuaDeclaration from '$/components/LuaDeclaration.astro';
import LuaProperty from '$/components/LuaProperty.astro';
import LuaSummary from '$/components/LuaSummary.astro';
import LuaParamTable from '$/components/LuaParamTable.astro';

Entry point for the DAVE (Discord Audio/Video Encryption) end-to-end encryption protocol.

DAVE provides authenticated encryption for Discord voice channels using MLS
(Messaging Layer Security). The typical lifecycle for a client joining a voice channel is:

1. Create a `Session` and call `init` with the protocol version and channel group ID.
2. Call `setExternalSender` with the credential package provided by the voice server.
3. Receive the client's `getMarshalledKeyPackage` bytes and send them to the voice server
   as an MLS proposal.
4. Process incoming `processProposals` / `processCommit` / `processWelcome` messages
   relayed by the voice server to advance the group epoch.
5. After each epoch change, call `Session:getKeyRatchet` for each participant and pass
   the result to the appropriate `Encryptor` or `Decryptor`.
6. Pass every outgoing media frame through `Encryptor:encrypt` and every incoming frame
   through `Decryptor:decrypt`.

`setLogSinkCallback` can be used to route libdave's internal log output into the
application's own logging system.

<LuaSummary className="Dave" properties={[ { name: "sys", type: "Dave.Sys" }, { name: "Session", type: "Dave.Session" }, { name: "Encryptor", type: "Dave.Encryptor" }, { name: "Decryptor", type: "Dave.Decryptor" }, { name: "KeyRatchet", type: "Dave.KeyRatchet" }, { name: "CommitResult", type: "Dave.CommitResult" }, { name: "WelcomeResult", type: "Dave.WelcomeResult" }, { name: "Buffer", type: "Dave.Buffer" } ]} functions={[ { name: "setLogSinkCallback", args: [ { name: "callback", type: "(severity: number, message: string) -> ()" } ] } ]} />

## Properties

### sys

<LuaProperty name="Dave.sys" type="Dave.Sys" />

---
Raw FFI bindings and constants for libdave. Exposes codec IDs, media type constants,
result codes, and the underlying C function table. Prefer the higher-level classes
over calling `sys.lib` directly.

### Session

<LuaProperty name="Dave.Session" type="Dave.Session" />


### Encryptor

<LuaProperty name="Dave.Encryptor" type="Dave.Encryptor" />


### Decryptor

<LuaProperty name="Dave.Decryptor" type="Dave.Decryptor" />


### KeyRatchet

<LuaProperty name="Dave.KeyRatchet" type="Dave.KeyRatchet" />


### CommitResult

<LuaProperty name="Dave.CommitResult" type="Dave.CommitResult" />


### WelcomeResult

<LuaProperty name="Dave.WelcomeResult" type="Dave.WelcomeResult" />


### Buffer

<LuaProperty name="Dave.Buffer" type="Dave.Buffer" />

---
Internal FFI interoperability helpers. Not part of the public API.


## Functions

### setLogSinkCallback

> <LuaDeclaration name="setLogSinkCallback" className="Dave" args={[ { name: "callback", type: "(severity: number, message: string) -> ()" } ]} />

Registers a global callback that receives all log messages emitted by libdave.
`severity` maps to `Dave.sys.loggingSeverity` constants. Set this early in
startup if you want to capture verbose or debug output from the library.

<LuaParamTable params={[ { name: "callback", type: "(severity: number, message: string) -> ()" } ]} />


