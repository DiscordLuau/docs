---
title: Logger
description: DiscordLuau docs for Logger.
sidebar:
  order: 1
---

[//]: # (This file was @generated from moonwave comments. Do not edit by hand.)
[//]: # (To edit docs, change the source comments in the DiscordLuau repo.)

import LuaDeclaration from '$/components/LuaDeclaration.astro';
import LuaProperty from '$/components/LuaProperty.astro';
import LuaSummary from '$/components/LuaSummary.astro';
import LuaParamTable from '$/components/LuaParamTable.astro';

Logger, a simple logging implementation for Discord Luau.

This implementation is based on the following document:
	- https://www.sumologic.com/glossary/log-levels/

Please defer to the above document for when it is appropriate to use each log level.

<LuaSummary className="Logger" methods={[ { name: "setLogLevel", isMethod: true, args: [ { name: "logLevel", type: "LogLevel" } ] }, { name: "setSink", isMethod: true, args: [ { name: "sink", type: "(message: string) -> ()" } ] } ]} />

## Methods

### setLogLevel

> <LuaDeclaration name="setLogLevel" className="Logger" args={[ { name: "logLevel", type: "LogLevel" } ]} isMethod />

Function responsible for setting the log level of the logger. Log levels with an index below the
	configured level will be suppressed. For example, setting the level to "Warn" suppresses Debug,
	Info, and Notice, while Warn and above are still emitted.

<LuaParamTable params={[ { name: "logLevel", type: "LogLevel" } ]} />

### setSink

> <LuaDeclaration name="setSink" className="Logger" args={[ { name: "sink", type: "(message: string) -> ()" } ]} isMethod />

Replaces the default `print` output with a custom sink. The sink receives the fully-formatted
log line (including ANSI codes) as a single string. Useful for capturing output in tests.

<LuaParamTable params={[ { name: "sink", type: "(message: string) -> ()" } ]} />


