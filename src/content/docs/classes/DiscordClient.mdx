---
title: DiscordClient
description: DiscordLuau docs for DiscordClient.
---
[//]: # (This file was automatically compiled through the Generator luau code under this repository)
[//]: # (if you want to edit the documentation of discord-luau, please write changes)
[//]: # (to the discord-luau repository itself, we extract the comments from discord-luau and translate)
[//]: # (them into markdown files that Nextra can interpret!)

[//]: # (----- DOCUMENT IMPORTS ----- )

import LuaDeclaration from '$/components/LuaDeclaration.astro';
import StyleComponent from '$/components/StyleComponent.astro';

[//]: # (----- DOCUMENT DESCRIPTION ----- )

The base class that implements the various properties, methods, and events that developers can use to interact with the Discord API.

```lua
local DiscordLuau = require("../Submodules/DiscordLuau")

local DiscordSettingsBuilder = DiscordLuau.DiscordSettingsBuilder.new("BOT TOKEN")
local DiscordClient = DiscordLuau.DiscordClient.new(DiscordSettingsBuilder)
```

[//]: # (----- DOCUMENT PROPERTIES ----- )

## Properties

### discordUser

 ```lua
 DiscordClient.discordUser :: DiscordUser
```

---
					
### discordApplication

 ```lua
 DiscordClient.discordApplication :: DiscordApplication
```

---
					
### discordGateway

 ```lua
 DiscordClient.discordGateway :: DiscordGateway
```

---
	
### eventManager

 ```lua
 DiscordClient.eventManager :: EventManager
```

---
	

[//]: # (----- DOCUMENT METHODS ----- )

## Methods

### connectAsync

> <LuaDeclaration name="connectAsync" className="DiscordClient" isMethod args={[  ]}/>
Connects the current DiscordClient to the Discord API.

```lua
local DiscordClient = DiscordLuau.DiscordClient.new(DiscordSettingsBuilder)

DiscordClient:connectAsync():after(function()
	print("Connected to the Discord API!")
end)
```
### fetchGuildAsync

> <LuaDeclaration name="fetchGuildAsync" className="DiscordClient" isMethod args={[ { name: "guildId", type: "string" } ]}/>
Fetches a guild by its ID.
### fetchChannelAsync

> <LuaDeclaration name="fetchChannelAsync" className="DiscordClient" isMethod args={[ { name: "channelId", type: "string" } ]}/>
Fetches a channel by its ID.
### updatePresenceAsync

> <LuaDeclaration name="updatePresenceAsync" className="DiscordClient" isMethod args={[ { name: "discordPresence", type: "table" } ]}/>
Updates the client's presence.
### setVerbose

> <LuaDeclaration name="setVerbose" className="DiscordClient" isMethod args={[ { name: "verbose", type: "boolean" } ]}/>
Sets the verbosity level for logging.

[//]: # (----- DOCUMENT FUNCTIONS ----- )

## Functions

### new

> DiscordClient.new( `discordSettings` DiscordSettingsBuilder ) ->  DiscordClient 

Constructor function for the Discord Client, used to instantiate a Discord Client object.
	

```lua
local DiscordClient = DiscordLuau.DiscordClient.new(DiscordSettingsBuilder)

DiscordClient:connectAsync():after(function()
	print("Connected to the Discord API!")
end)
```

