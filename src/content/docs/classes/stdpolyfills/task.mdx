---
title: Task
description: DiscordLuau docs for Task.
sidebar:
  order: 100
---

[//]: # (This file was @generated from moonwave comments. Do not edit by hand.)
[//]: # (To edit docs, change the source comments in the DiscordLuau repo.)

import LuaDeclaration from '$/components/LuaDeclaration.astro';
import LuaProperty from '$/components/LuaProperty.astro';



## Functions

### cancel

> <LuaDeclaration name="cancel" className="Task" args={[ { name: "thread", type: "thread" } ]} />

Stops a currently scheduled thread from resuming.

### defer

> <LuaDeclaration name="defer" className="Task" args={[ { name: "functionOrThread", type: "thread | (T...) -> ...any" }, { name: "...", type: "T..." } ]} returnType="thread" />

Defers a thread or function to run at the end of the current task queue.

### delay

> <LuaDeclaration name="delay" className="Task" args={[ { name: "duration", type: "number" }, { name: "functionOrThread", type: "thread | (T...) -> ...any" }, { name: "...", type: "T..." } ]} returnType="thread" />

Delays a thread or function to run after duration seconds.

### spawn

> <LuaDeclaration name="spawn" className="Task" args={[ { name: "functionOrThread", type: "thread | (T...) -> ...any" }, { name: "...", type: "T..." } ]} returnType="thread" />

Instantly runs a thread or function.

If the spawned task yields, the thread that spawned the task will resume, letting the spawned task run in the
	background.

### wait

> <LuaDeclaration name="wait" className="Task" args={[ { name: "duration", type: "number?" } ]} returnType="number" />

Waits for at least the given amount of time.

The minimum wait time possible when using task.wait is limited by the underlying OS sleep implementation. For most
	systems this means task.wait is accurate down to about 5 milliseconds or less.


