---
title: Process
description: DiscordLuau docs for Process.
sidebar:
  order: 100
---

[//]: # (This file was @generated from moonwave comments. Do not edit by hand.)
[//]: # (To edit docs, change the source comments in the DiscordLuau repo.)

import LuaDeclaration from '$/components/LuaDeclaration.astro';
import LuaProperty from '$/components/LuaProperty.astro';
import LuaSummary from '$/components/LuaSummary.astro';
import LuaParamTable from '$/components/LuaParamTable.astro';



<LuaSummary className="Process" functions={[ { name: "getPlatform", args: [  ], returnType: "OS" }, { name: "getArgs", args: [  ], returnType: "{ string }" }, { name: "getEnv", args: [  ], returnType: "{ [string]: string }" }, { name: "getCwd", args: [  ], returnType: "string" }, { name: "run", args: [ { name: "exec", type: "string" }, { name: "args", type: "{ string }?" }, { name: "opts", type: "{ cwd: string?, env: { [string]: string }? }?" } ], returnType: "RunResult" }, { name: "getArch", args: [  ], returnType: "Arch" }, { name: "onSignal", args: [ { name: "signal", type: "Signal" }, { name: "callback", type: "() -> ()" } ] } ]} />

## Functions

### getPlatform

> <LuaDeclaration name="getPlatform" className="Process" args={[  ]} returnType="OS" />

Returns the current OS thats being used

<LuaParamTable returns={[ { type: "OS" } ]} />

### getArgs

> <LuaDeclaration name="getArgs" className="Process" args={[  ]} returnType="{ string }" />

Returns the arguments passed into the current program

<LuaParamTable returns={[ { type: "{ string }" } ]} />

### getEnv

> <LuaDeclaration name="getEnv" className="Process" args={[  ]} returnType="{ [string]: string }" />

Returns the environment the current process is executing under

<LuaParamTable returns={[ { type: "{ [string]: string }" } ]} />

### getCwd

> <LuaDeclaration name="getCwd" className="Process" args={[  ]} returnType="string" />

Returns the current working directory for this process

<LuaParamTable returns={[ { type: "string" } ]} />

### run

> <LuaDeclaration name="run" className="Process" args={[ { name: "exec", type: "string" }, { name: "args", type: "{ string }?" }, { name: "opts", type: "{ cwd: string?, env: { [string]: string }? }?" } ]} returnType="RunResult" />

Runs a subprocess and returns the result. Only supported on Zune; calling from
Lune will raise an error explaining the requirement.

<LuaParamTable params={[ { name: "exec", type: "string" }, { name: "args", type: "{ string }?" }, { name: "opts", type: "{ cwd: string?, env: { [string]: string }? }?" } ]} returns={[ { type: "RunResult" } ]} />

### getArch

> <LuaDeclaration name="getArch" className="Process" args={[  ]} returnType="Arch" />

Returns the CPU architecture the current process is executing on

<LuaParamTable returns={[ { type: "Arch" } ]} />

### onSignal

> <LuaDeclaration name="onSignal" className="Process" args={[ { name: "signal", type: "Signal" }, { name: "callback", type: "() -> ()" } ]} />

Registers a callback to be invoked when the process receives an OS signal (e.g. `"INT"` for SIGINT).
Only supported on Zune; on Lune this is a no-op with a warning.

<LuaParamTable params={[ { name: "signal", type: "Signal" }, { name: "callback", type: "() -> ()" } ]} />


