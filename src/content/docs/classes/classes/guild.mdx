---
title: Guild
description: DiscordLuau docs for Guild.
sidebar:
  order: 100
---

[//]: # (This file was @generated from moonwave comments. Do not edit by hand.)
[//]: # (To edit docs, change the source comments in the DiscordLuau repo.)

import LuaDeclaration from '$/components/LuaDeclaration.astro';
import LuaProperty from '$/components/LuaProperty.astro';
import LuaSummary from '$/components/LuaSummary.astro';
import LuaParamTable from '$/components/LuaParamTable.astro';

Implementation of the Discord Guild class in Luau.
Represents a Discord guild (server) with all its properties and methods.

https://discord.com/developers/docs/resources/guild#guild-object

<LuaSummary className="Guild" properties={[ { name: "stickers", type: "{Classes.Sticker}" }, { name: "roles", type: "{Classes.Guild.Role}" }, { name: "emojis", type: "{Classes.Emoji}" }, { name: "verificationLevel", type: "string" }, { name: "defaultMessageNotifications", type: "string" }, { name: "explicitContentFilter", type: "string" }, { name: "premiumTier", type: "string" }, { name: "nsfwLevel", type: "string" }, { name: "mfaLevel", type: "string" }, { name: "systemChannelFlags", type: "Classes.SystemChannelBitflag" }, { name: "icon", type: "string?" }, { name: "iconHash", type: "string?" }, { name: "name", type: "string?" }, { name: "splash", type: "string?" }, { name: "discoverySplash", type: "string?" }, { name: "owner", type: "boolean?" }, { name: "ownerId", type: "string?" }, { name: "permissions", type: "string?" }, { name: "region", type: "string?" }, { name: "afkChannelId", type: "string?" }, { name: "afkTimeout", type: "number?" }, { name: "widgetEnabled", type: "boolean?" }, { name: "widgetChannelId", type: "string?" }, { name: "features", type: "{string}?" }, { name: "applicationId", type: "string?" }, { name: "systemChannelId", type: "string?" }, { name: "rulesChannelId", type: "string?" }, { name: "maxPresences", type: "number?" }, { name: "maxMembers", type: "number?" }, { name: "vanityUrlCode", type: "string?" }, { name: "description", type: "string?" }, { name: "banner", type: "string?" }, { name: "premiumSubscriptionCount", type: "number?" }, { name: "preferredLocale", type: "string?" }, { name: "publicUpdatesChannelId", type: "string?" }, { name: "maxVideoChannelUsers", type: "number?" }, { name: "maxStageVideoChannelUsers", type: "number?" }, { name: "approximateMemberCount", type: "number?" }, { name: "approximatePresenceCount", type: "number?" }, { name: "premiumProgressBarEnabled", type: "boolean?" }, { name: "safetyAlertsChannelId", type: "string?" } ]} methods={[ { name: "createSlashCommandAsync", isMethod: true, args: [ { name: "slashCommand", type: "builders.interactionJSON" } ] }, { name: "deleteSlashCommandAsync", isMethod: true, args: [ { name: "slashCommandId", type: "apiTypes.Snowflake" } ] }, { name: "editSlashCommandAsync", isMethod: true, args: [ { name: "slashCommandId", type: "apiTypes.Snowflake" }, { name: "slashCommand", type: "builders.interactionJSON" } ] }, { name: "getSlashCommandsAsync", isMethod: true, args: [ { name: "withLocalizations", type: "boolean?" } ] }, { name: "overwriteSlashCommandsAsync", isMethod: true, args: [ { name: "slashCommands", type: "{ builders.interactionJSON }" } ] }, { name: "getPreviewAsync", isMethod: true, args: [  ] }, { name: "modifyAsync", isMethod: true, args: [ { name: "json", type: "builders.guildJSON" }, { name: "reason", type: "string?" } ] }, { name: "deleteAsync", isMethod: true, args: [  ] }, { name: "getChannelsAsync", isMethod: true, args: [  ] }, { name: "getTextChannelsAsync", isMethod: true, args: [  ] }, { name: "getVoiceChannelsAsync", isMethod: true, args: [  ] }, { name: "getCategoryChannelsAsync", isMethod: true, args: [  ] }, { name: "getAnnouncementChannelsAsync", isMethod: true, args: [  ] }, { name: "getStageVoiceChannelsAsync", isMethod: true, args: [  ] }, { name: "getDirectoryChannelsAsync", isMethod: true, args: [  ] }, { name: "createChannelAsync", isMethod: true, args: [ { name: "json", type: "builders.channelJSON" }, { name: "reason", type: "string?" } ] }, { name: "getRoleMemberCounts", isMethod: true, args: [  ] }, { name: "getThreadsAsync", isMethod: true, args: [  ] }, { name: "getAnnouncementThreadsAsync", isMethod: true, args: [  ] }, { name: "getPublicThreadsAsync", isMethod: true, args: [  ] }, { name: "getPrivateThreadsAsync", isMethod: true, args: [  ] }, { name: "getMemberAsync", isMethod: true, args: [ { name: "userId", type: "string" } ] }, { name: "listMembersAsync", isMethod: true, args: [ { name: "lastSnowflake", type: "string?" }, { name: "limit", type: "number?" } ] }, { name: "searchMembersAsync", isMethod: true, args: [ { name: "query", type: "string" }, { name: "limit", type: "number?" } ] }, { name: "addMemberAsync", isMethod: true, args: [ { name: "userId", type: "apiTypes.Snowflake" }, { name: "accessToken", type: "string" }, { name: "nickname", type: "string?" }, { name: "roles", type: "{ apiTypes.Snowflake }?" }, { name: "muted", type: "boolean?" }, { name: "deafened", type: "boolean?" } ] }, { name: "kickMemberAsync", isMethod: true, args: [ { name: "userId", type: "apiTypes.Snowflake" }, { name: "reason", type: "string?" } ] }, { name: "banMemberAsync", isMethod: true, args: [ { name: "userId", type: "apiTypes.Snowflake" }, { name: "reason", type: "string?" }, { name: "deleteMessageDays", type: "number?" }, { name: "deleteMessageSeconds", type: "number?" } ] }, { name: "bulkBanAsync", isMethod: true, args: [ { name: "userIds", type: "{ apiTypes.Snowflake }" }, { name: "reason", type: "string?" }, { name: "deleteMessageSeconds", type: "number?" } ], returnType: "future.Future< string, { failedUsers: { apiTypes.Snowflake }, bannedUsers: { apiTypes.Snowflake } } >" }, { name: "getBansAsync", isMethod: true, args: [ { name: "limit", type: "number?" }, { name: "before", type: "apiTypes.Snowflake?" }, { name: "after", type: "apiTypes.Snowflake?" } ] }, { name: "getBanAsync", isMethod: true, args: [ { name: "userId", type: "apiTypes.Snowflake" } ] }, { name: "getRolesAsync", isMethod: true, args: [  ] }, { name: "getRoleAsync", isMethod: true, args: [ { name: "roleId", type: "apiTypes.Snowflake" } ] }, { name: "createRoleAsync", isMethod: true, args: [ { name: "roleJson", type: "builders.roleJSON" }, { name: "reason", type: "string?" } ] }, { name: "setMFALevelAsync", isMethod: true, args: [ { name: "mfaLevel", type: "apiTypes.MFALevel" }, { name: "reason", type: "string?" } ] }, { name: "getPruneCountAsync", isMethod: true, args: [ { name: "days", type: "number?" }, { name: "includedRoles", type: "{ apiTypes.Snowflake }?" } ] }, { name: "beginPruneAsync", isMethod: true, args: [ { name: "days", type: "number?" }, { name: "returnPruneCount", type: "boolean?" }, { name: "includedRoles", type: "{ apiTypes.Snowflake }?" }, { name: "reason", type: "string?" } ] }, { name: "getVoiceRegionsAsync", isMethod: true, args: [  ] }, { name: "getInvitesAsync", isMethod: true, args: [  ] }, { name: "getIntegrationsAsync", isMethod: true, args: [  ] }, { name: "deleteIntegrationAsync", isMethod: true, args: [ { name: "integrationId", type: "string" } ] }, { name: "getWidgetSettingsAsync", isMethod: true, args: [  ] }, { name: "modifyWidgetAsync", isMethod: true, args: [ { name: "json", type: "builders.widgetSettingsJSON" }, { name: "reason", type: "string?" } ] }, { name: "getWidgetAsync", isMethod: true, args: [  ] }, { name: "getVanityUrlAsync", isMethod: true, args: [  ] }, { name: "getWidgetImageAsync", isMethod: true, args: [ { name: "style", type: "&quot;shield&quot; | &quot;banner1&quot; | &quot;banner2&quot; | &quot;banner3&quot; | &quot;banner4&quot;" } ] }, { name: "getWelcomeScreenAsync", isMethod: true, args: [  ] }, { name: "modifyWelcomeScreenAsync", isMethod: true, args: [ { name: "json", type: "builders.welcomeScreenJSON" }, { name: "reason", type: "string?" } ] }, { name: "getOnboardingAsync", isMethod: true, args: [  ] }, { name: "modifyOnboardingAsync", isMethod: true, args: [ { name: "json", type: "builders.onboardingJSON" }, { name: "reason", type: "string?" } ] }, { name: "getGuildAuditLogAsync", isMethod: true, args: [ { name: "userId", type: "apiTypes.Snowflake?" }, { name: "actionType", type: "apiTypes.ActionTypes?" }, { name: "before", type: "apiTypes.Snowflake?" }, { name: "after", type: "apiTypes.Snowflake?" }, { name: "limit", type: "number?" } ] }, { name: "listAutomoderationRulesAsync", isMethod: true, args: [  ] }, { name: "getAutomoderationRuleAsync", isMethod: true, args: [ { name: "ruleId", type: "apiTypes.Snowflake" } ] }, { name: "createAutomoderationRuleAsync", isMethod: true, args: [ { name: "ruleJSON", type: "builders.AutomoderationRuleJSON" }, { name: "reason", type: "string?" } ] } ]} />

## Properties

### stickers

<LuaProperty name="Guild.stickers" type="{Classes.Sticker}" />

---
Collection of stickers in the guild

### roles

<LuaProperty name="Guild.roles" type="{Classes.Guild.Role}" />

---
Collection of roles in the guild

### emojis

<LuaProperty name="Guild.emojis" type="{Classes.Emoji}" />

---
Collection of emojis in the guild

### verificationLevel

<LuaProperty name="Guild.verificationLevel" type="string" />

---
Verification level required for the guild

### defaultMessageNotifications

<LuaProperty name="Guild.defaultMessageNotifications" type="string" />

---
Default message notifications level

### explicitContentFilter

<LuaProperty name="Guild.explicitContentFilter" type="string" />

---
Explicit content filter level

### premiumTier

<LuaProperty name="Guild.premiumTier" type="string" />

---
Premium tier (Server Boost level)

### nsfwLevel

<LuaProperty name="Guild.nsfwLevel" type="string" />

---
NSFW level of the guild

### mfaLevel

<LuaProperty name="Guild.mfaLevel" type="string" />

---
Required MFA level for the guild

### systemChannelFlags

<LuaProperty name="Guild.systemChannelFlags" type="Classes.SystemChannelBitflag" />

---
System channel flags

### icon

<LuaProperty name="Guild.icon" type="string?" />

---
Icon hash of the guild

### iconHash

<LuaProperty name="Guild.iconHash" type="string?" />

---
The hash of the guild's icon

### name

<LuaProperty name="Guild.name" type="string?" />

---
The name of the guild

### splash

<LuaProperty name="Guild.splash" type="string?" />

---
Splash hash

### discoverySplash

<LuaProperty name="Guild.discoverySplash" type="string?" />

---
Discovery splash hash

### owner

<LuaProperty name="Guild.owner" type="boolean?" />

---
Whether the user is the owner of the guild

### ownerId

<LuaProperty name="Guild.ownerId" type="string?" />

---
ID of guild owner

### permissions

<LuaProperty name="Guild.permissions" type="string?" />

---
Total permissions for the user in the guild (excludes overwrites)

### region

<LuaProperty name="Guild.region" type="string?" />

---
Voice region id for the guild

### afkChannelId

<LuaProperty name="Guild.afkChannelId" type="string?" />

---
ID of AFK channel

### afkTimeout

<LuaProperty name="Guild.afkTimeout" type="number?" />

---
AFK timeout in seconds

### widgetEnabled

<LuaProperty name="Guild.widgetEnabled" type="boolean?" />

---
Whether the server widget is enabled

### widgetChannelId

<LuaProperty name="Guild.widgetChannelId" type="string?" />

---
The channel id that the widget will generate an invite to

### features

<LuaProperty name="Guild.features" type="{string}?" />

---
Array of guild features

### applicationId

<LuaProperty name="Guild.applicationId" type="string?" />

---
Application ID of the guild creator if it is bot-created

### systemChannelId

<LuaProperty name="Guild.systemChannelId" type="string?" />

---
The id of the channel where guild notices are posted

### rulesChannelId

<LuaProperty name="Guild.rulesChannelId" type="string?" />

---
ID of the rules channel

### maxPresences

<LuaProperty name="Guild.maxPresences" type="number?" />

---
The maximum number of presences for the guild

### maxMembers

<LuaProperty name="Guild.maxMembers" type="number?" />

---
The maximum number of members for the guild

### vanityUrlCode

<LuaProperty name="Guild.vanityUrlCode" type="string?" />

---
The vanity URL code for the guild

### description

<LuaProperty name="Guild.description" type="string?" />

---
The description of the guild

### banner

<LuaProperty name="Guild.banner" type="string?" />

---
Banner hash

### premiumSubscriptionCount

<LuaProperty name="Guild.premiumSubscriptionCount" type="number?" />

---
The number of boosts this guild currently has

### preferredLocale

<LuaProperty name="Guild.preferredLocale" type="string?" />

---
The preferred locale of a Community guild

### publicUpdatesChannelId

<LuaProperty name="Guild.publicUpdatesChannelId" type="string?" />

---
The id of the channel where admins and moderators receive notices from Discord

### maxVideoChannelUsers

<LuaProperty name="Guild.maxVideoChannelUsers" type="number?" />

---
The maximum amount of users in a video channel

### maxStageVideoChannelUsers

<LuaProperty name="Guild.maxStageVideoChannelUsers" type="number?" />

---
The maximum amount of users in a stage video channel

### approximateMemberCount

<LuaProperty name="Guild.approximateMemberCount" type="number?" />

---
Approximate number of members in this guild

### approximatePresenceCount

<LuaProperty name="Guild.approximatePresenceCount" type="number?" />

---
Approximate number of non-offline members in this guild

### premiumProgressBarEnabled

<LuaProperty name="Guild.premiumProgressBarEnabled" type="boolean?" />

---
Whether the guild has the boost progress bar enabled

### safetyAlertsChannelId

<LuaProperty name="Guild.safetyAlertsChannelId" type="string?" />

---
The id of the channel where admins and moderators receive safety alerts from Discord


## Methods

### createSlashCommandAsync

> <LuaDeclaration name="createSlashCommandAsync" className="Guild" args={[ { name: "slashCommand", type: "builders.interactionJSON" } ]} isMethod />

Creates a guild Slash command for the application.

<LuaParamTable params={[ { name: "slashCommand", type: "builders.interactionJSON" } ]} />

### deleteSlashCommandAsync

> <LuaDeclaration name="deleteSlashCommandAsync" className="Guild" args={[ { name: "slashCommandId", type: "apiTypes.Snowflake" } ]} isMethod />

Delete an existing guild application command.

<LuaParamTable params={[ { name: "slashCommandId", type: "apiTypes.Snowflake" } ]} />

### editSlashCommandAsync

> <LuaDeclaration name="editSlashCommandAsync" className="Guild" args={[ { name: "slashCommandId", type: "apiTypes.Snowflake" }, { name: "slashCommand", type: "builders.interactionJSON" } ]} isMethod />

Edit an existing guild application command.

<LuaParamTable params={[ { name: "slashCommandId", type: "apiTypes.Snowflake" }, { name: "slashCommand", type: "builders.interactionJSON" } ]} />

### getSlashCommandsAsync

> <LuaDeclaration name="getSlashCommandsAsync" className="Guild" args={[ { name: "withLocalizations", type: "boolean?" } ]} isMethod />

Get a list of guild application commands.

<LuaParamTable params={[ { name: "withLocalizations", type: "boolean?" } ]} />

### overwriteSlashCommandsAsync

> <LuaDeclaration name="overwriteSlashCommandsAsync" className="Guild" args={[ { name: "slashCommands", type: "{ builders.interactionJSON }" } ]} isMethod />

Overwrite all guild application commands. This will skip over commands that are the same as the current commands,
and will not remove any existing commands.

<LuaParamTable params={[ { name: "slashCommands", type: "{ builders.interactionJSON }" } ]} />

### getPreviewAsync

> <LuaDeclaration name="getPreviewAsync" className="Guild" args={[  ]} isMethod />

Returns the guild preview object for the given id. If the user is not in the guild, then the guild must be
discoverable.

### modifyAsync

> <LuaDeclaration name="modifyAsync" className="Guild" args={[ { name: "json", type: "builders.guildJSON" }, { name: "reason", type: "string?" } ]} isMethod />

Modify a guild's settings. Requires the MANAGE_GUILD permission.
Returns the updated guild object on success. Fires a Guild Update Gateway event.

<LuaParamTable params={[ { name: "json", type: "builders.guildJSON" }, { name: "reason", type: "string?" } ]} />

### deleteAsync

> <LuaDeclaration name="deleteAsync" className="Guild" args={[  ]} isMethod />

Delete a guild permanently. User must be owner.
Returns 204 No Content on success. Fires a Guild Delete Gateway event.

### getChannelsAsync

> <LuaDeclaration name="getChannelsAsync" className="Guild" args={[  ]} isMethod />

Returns a list of guild channel objects. Does not include threads.

Typed as unknown because the API returns a variety of channel types, and luau's type system is not currently
capable of handling this.

Please use explicit functions for specific channel types, for example: getTextChannelsAsync, getVoiceChannelsAsync, etc.

### getTextChannelsAsync

> <LuaDeclaration name="getTextChannelsAsync" className="Guild" args={[  ]} isMethod />

Returns a list of guild text channel objects.

### getVoiceChannelsAsync

> <LuaDeclaration name="getVoiceChannelsAsync" className="Guild" args={[  ]} isMethod />

Returns a list of guild voice channel objects.

### getCategoryChannelsAsync

> <LuaDeclaration name="getCategoryChannelsAsync" className="Guild" args={[  ]} isMethod />

Returns a list of guild category channel objects.

### getAnnouncementChannelsAsync

> <LuaDeclaration name="getAnnouncementChannelsAsync" className="Guild" args={[  ]} isMethod />

Returns a list of guild announcement channel objects.

### getStageVoiceChannelsAsync

> <LuaDeclaration name="getStageVoiceChannelsAsync" className="Guild" args={[  ]} isMethod />

Returns a list of guild stage voice channel objects.

### getDirectoryChannelsAsync

> <LuaDeclaration name="getDirectoryChannelsAsync" className="Guild" args={[  ]} isMethod />

Returns a list of guild directory channel objects.

### createChannelAsync

> <LuaDeclaration name="createChannelAsync" className="Guild" args={[ { name: "json", type: "builders.channelJSON" }, { name: "reason", type: "string?" } ]} isMethod />

Create a new channel object for the guild. Requires the MANAGE_CHANNELS permission.
If setting permission overwrites, only permissions your bot has in the guild can be allowed/denied.
Setting MANAGE_ROLES permission in channels is only possible for guild administrators.
Returns the new channel object on success. Fires a Channel Create Gateway event.

<LuaParamTable params={[ { name: "json", type: "builders.channelJSON" }, { name: "reason", type: "string?" } ]} />

### getRoleMemberCounts

> <LuaDeclaration name="getRoleMemberCounts" className="Guild" args={[  ]} isMethod />

Returns a map of role IDs to the number of members with the role. Does not include the @everyone role.

### getThreadsAsync

> <LuaDeclaration name="getThreadsAsync" className="Guild" args={[  ]} isMethod />

Returns all active threads in the guild, including public and private threads.
Threads are ordered by their id, in descending order.

Typed as unknown because the API returns a variety of channel types, and luau's type system is not currently
capable of handling this.

Please use explicit functions for specific channel types, for example: getMediaThreadsAsync, getAnnouncementThreadsAsync, etc.

### getAnnouncementThreadsAsync

> <LuaDeclaration name="getAnnouncementThreadsAsync" className="Guild" args={[  ]} isMethod />

Returns a list of guild announcement thread objects.

### getPublicThreadsAsync

> <LuaDeclaration name="getPublicThreadsAsync" className="Guild" args={[  ]} isMethod />

Returns a list of public guild thread objects.

### getPrivateThreadsAsync

> <LuaDeclaration name="getPrivateThreadsAsync" className="Guild" args={[  ]} isMethod />

Returns a list of private guild thread objects.

### getMemberAsync

> <LuaDeclaration name="getMemberAsync" className="Guild" args={[ { name: "userId", type: "string" } ]} isMethod />

Returns a guild member object for the specified user.

<LuaParamTable params={[ { name: "userId", type: "string" } ]} />

### listMembersAsync

> <LuaDeclaration name="listMembersAsync" className="Guild" args={[ { name: "lastSnowflake", type: "string?" }, { name: "limit", type: "number?" } ]} isMethod />

Returns a list of guild member objects that are members of the guild.

<LuaParamTable params={[ { name: "lastSnowflake", type: "string?" }, { name: "limit", type: "number?" } ]} />

### searchMembersAsync

> <LuaDeclaration name="searchMembersAsync" className="Guild" args={[ { name: "query", type: "string" }, { name: "limit", type: "number?" } ]} isMethod />

Returns a list of guild member objects whose username or nickname starts with a provided string.

<LuaParamTable params={[ { name: "query", type: "string" }, { name: "limit", type: "number?" } ]} />

### addMemberAsync

> <LuaDeclaration name="addMemberAsync" className="Guild" args={[ { name: "userId", type: "apiTypes.Snowflake" }, { name: "accessToken", type: "string" }, { name: "nickname", type: "string?" }, { name: "roles", type: "{ apiTypes.Snowflake }?" }, { name: "muted", type: "boolean?" }, { name: "deafened", type: "boolean?" } ]} isMethod />

Adds a user to the guild, provided you have a valid oauth2 access token for the user with the guilds.join scope.

For guilds with Membership Screening enabled, this endpoint will default to adding new members as pending in the
guild member object. Members that are pending will have to complete membership screening before they become full
members that can talk.

<LuaParamTable params={[ { name: "userId", type: "apiTypes.Snowflake" }, { name: "accessToken", type: "string" }, { name: "nickname", type: "string?" }, { name: "roles", type: "{ apiTypes.Snowflake }?" }, { name: "muted", type: "boolean?" }, { name: "deafened", type: "boolean?" } ]} />

### kickMemberAsync

> <LuaDeclaration name="kickMemberAsync" className="Guild" args={[ { name: "userId", type: "apiTypes.Snowflake" }, { name: "reason", type: "string?" } ]} isMethod />

Removes a role from a guild member. Requires the MANAGE_ROLES permission.

<LuaParamTable params={[ { name: "userId", type: "apiTypes.Snowflake" }, { name: "reason", type: "string?" } ]} />

### banMemberAsync

> <LuaDeclaration name="banMemberAsync" className="Guild" args={[ { name: "userId", type: "apiTypes.Snowflake" }, { name: "reason", type: "string?" }, { name: "deleteMessageDays", type: "number?" }, { name: "deleteMessageSeconds", type: "number?" } ]} isMethod />

Create a guild ban, and optionally delete previous messages sent by the banned user.

<LuaParamTable params={[ { name: "userId", type: "apiTypes.Snowflake" }, { name: "reason", type: "string?" }, { name: "deleteMessageDays", type: "number?" }, { name: "deleteMessageSeconds", type: "number?" } ]} />

### bulkBanAsync

> <LuaDeclaration name="bulkBanAsync" className="Guild" args={[ { name: "userIds", type: "{ apiTypes.Snowflake }" }, { name: "reason", type: "string?" }, { name: "deleteMessageSeconds", type: "number?" } ]} returnType="future.Future< string, { failedUsers: { apiTypes.Snowflake }, bannedUsers: { apiTypes.Snowflake } } >" isMethod />

Create a guild ban for multiple users, and optionally delete previous messages sent by the banned users.

<LuaParamTable params={[ { name: "userIds", type: "{ apiTypes.Snowflake }" }, { name: "reason", type: "string?" }, { name: "deleteMessageSeconds", type: "number?" } ]} returns={[ { type: "future.Future< string, { failedUsers: { apiTypes.Snowflake }, bannedUsers: { apiTypes.Snowflake } } >" } ]} />

### getBansAsync

> <LuaDeclaration name="getBansAsync" className="Guild" args={[ { name: "limit", type: "number?" }, { name: "before", type: "apiTypes.Snowflake?" }, { name: "after", type: "apiTypes.Snowflake?" } ]} isMethod />

Returns a list of ban objects for the users banned from this guild.

<LuaParamTable params={[ { name: "limit", type: "number?" }, { name: "before", type: "apiTypes.Snowflake?" }, { name: "after", type: "apiTypes.Snowflake?" } ]} />

### getBanAsync

> <LuaDeclaration name="getBanAsync" className="Guild" args={[ { name: "userId", type: "apiTypes.Snowflake" } ]} isMethod />

Returns a ban object for the given user or a 404 not found if the ban cannot be found.

<LuaParamTable params={[ { name: "userId", type: "apiTypes.Snowflake" } ]} />

### getRolesAsync

> <LuaDeclaration name="getRolesAsync" className="Guild" args={[  ]} isMethod />

Returns a list of role objects for the guild.

### getRoleAsync

> <LuaDeclaration name="getRoleAsync" className="Guild" args={[ { name: "roleId", type: "apiTypes.Snowflake" } ]} isMethod />

Returns a role object for the specified role.

<LuaParamTable params={[ { name: "roleId", type: "apiTypes.Snowflake" } ]} />

### createRoleAsync

> <LuaDeclaration name="createRoleAsync" className="Guild" args={[ { name: "roleJson", type: "builders.roleJSON" }, { name: "reason", type: "string?" } ]} isMethod />

Create a new role for the guild. Requires the MANAGE_ROLES permission.
Returns the new role object on success.

<LuaParamTable params={[ { name: "roleJson", type: "builders.roleJSON" }, { name: "reason", type: "string?" } ]} />

### setMFALevelAsync

> <LuaDeclaration name="setMFALevelAsync" className="Guild" args={[ { name: "mfaLevel", type: "apiTypes.MFALevel" }, { name: "reason", type: "string?" } ]} isMethod />

Modify a guild's MFA level. Requires guild ownership. Returns the updated level on success.

<LuaParamTable params={[ { name: "mfaLevel", type: "apiTypes.MFALevel" }, { name: "reason", type: "string?" } ]} />

### getPruneCountAsync

> <LuaDeclaration name="getPruneCountAsync" className="Guild" args={[ { name: "days", type: "number?" }, { name: "includedRoles", type: "{ apiTypes.Snowflake }?" } ]} isMethod />

Returns an object with one pruned key indicating the number of members that would be removed in a prune operation.
Requires the MANAGE_GUILD and KICK_MEMBERS permissions.

By default, prune will not remove users with roles. You can optionally include specific roles in your prune by
providing the include_roles parameter. Any inactive user that has a subset of the provided role(s) will be
counted in the prune and users with additional roles will not.

<LuaParamTable params={[ { name: "days", type: "number?" }, { name: "includedRoles", type: "{ apiTypes.Snowflake }?" } ]} />

### beginPruneAsync

> <LuaDeclaration name="beginPruneAsync" className="Guild" args={[ { name: "days", type: "number?" }, { name: "returnPruneCount", type: "boolean?" }, { name: "includedRoles", type: "{ apiTypes.Snowflake }?" }, { name: "reason", type: "string?" } ]} isMethod />

Begin a prune operation. Requires the MANAGE_GUILD and KICK_MEMBERS permissions.
Returns an object with one pruned key indicating the number of members that were removed in the prune operation.

For large guilds it's recommended to set the compute_prune_count option to false, forcing pruned to null

By default, prune will not remove users with roles. You can optionally include specific roles in your prune by
providing the include_roles parameter. Any inactive user that has a subset of the provided role(s)
will be included in the prune and users with additional roles will not.

<LuaParamTable params={[ { name: "days", type: "number?" }, { name: "returnPruneCount", type: "boolean?" }, { name: "includedRoles", type: "{ apiTypes.Snowflake }?" }, { name: "reason", type: "string?" } ]} />

### getVoiceRegionsAsync

> <LuaDeclaration name="getVoiceRegionsAsync" className="Guild" args={[  ]} isMethod />

Returns an array of voice region objects that can be used when setting a voice or stage channel's rtc_region.

### getInvitesAsync

> <LuaDeclaration name="getInvitesAsync" className="Guild" args={[  ]} isMethod />

Returns a list of invite objects (with invite metadata) for the guild. Requires the MANAGE_GUILD permission.

### getIntegrationsAsync

> <LuaDeclaration name="getIntegrationsAsync" className="Guild" args={[  ]} isMethod />

Returns a list of integration objects for the guild. Requires the MANAGE_GUILD permission.

### deleteIntegrationAsync

> <LuaDeclaration name="deleteIntegrationAsync" className="Guild" args={[ { name: "integrationId", type: "string" } ]} isMethod />

Delete the attached integration object for the guild. Deletes any associated webhooks and kicks the associated
bot if there is one.

Requires the MANAGE_GUILD permission.

<LuaParamTable params={[ { name: "integrationId", type: "string" } ]} />

### getWidgetSettingsAsync

> <LuaDeclaration name="getWidgetSettingsAsync" className="Guild" args={[  ]} isMethod />

Returns a guild widget settings object. Requires the MANAGE_GUILD permission.

### modifyWidgetAsync

> <LuaDeclaration name="modifyWidgetAsync" className="Guild" args={[ { name: "json", type: "builders.widgetSettingsJSON" }, { name: "reason", type: "string?" } ]} isMethod />

Modify a guild widget settings object for the guild. All attributes may be passed in with JSON and modified.
Requires the MANAGE_GUILD permission. Returns the updated guild widget settings object. 

<LuaParamTable params={[ { name: "json", type: "builders.widgetSettingsJSON" }, { name: "reason", type: "string?" } ]} />

### getWidgetAsync

> <LuaDeclaration name="getWidgetAsync" className="Guild" args={[  ]} isMethod />

Returns the widget for the guild. Fires an Invite Create Gateway event when an invite channel is defined
and a new Invite is generated.

### getVanityUrlAsync

> <LuaDeclaration name="getVanityUrlAsync" className="Guild" args={[  ]} isMethod />

Returns a partial invite object for guilds with that feature enabled. Requires the MANAGE_GUILD permission.
code will be null if a vanity url for the guild is not set.

### getWidgetImageAsync

> <LuaDeclaration name="getWidgetImageAsync" className="Guild" args={[ { name: "style", type: "&quot;shield&quot; | &quot;banner1&quot; | &quot;banner2&quot; | &quot;banner3&quot; | &quot;banner4&quot;" } ]} isMethod />

Returns a PNG image widget for the guild. Requires no permissions or authentication.

<LuaParamTable params={[ { name: "style", type: "&quot;shield&quot; | &quot;banner1&quot; | &quot;banner2&quot; | &quot;banner3&quot; | &quot;banner4&quot;" } ]} />

### getWelcomeScreenAsync

> <LuaDeclaration name="getWelcomeScreenAsync" className="Guild" args={[  ]} isMethod />

Returns the Welcome Screen object for the guild. If the welcome screen is not enabled.

the MANAGE_GUILD permission is required.

### modifyWelcomeScreenAsync

> <LuaDeclaration name="modifyWelcomeScreenAsync" className="Guild" args={[ { name: "json", type: "builders.welcomeScreenJSON" }, { name: "reason", type: "string?" } ]} isMethod />

Modify the guild's Welcome Screen. Requires the MANAGE_GUILD permission.

<LuaParamTable params={[ { name: "json", type: "builders.welcomeScreenJSON" }, { name: "reason", type: "string?" } ]} />

### getOnboardingAsync

> <LuaDeclaration name="getOnboardingAsync" className="Guild" args={[  ]} isMethod />

Returns the Onboarding object for the guild.

### modifyOnboardingAsync

> <LuaDeclaration name="modifyOnboardingAsync" className="Guild" args={[ { name: "json", type: "builders.onboardingJSON" }, { name: "reason", type: "string?" } ]} isMethod />

Modifies the onboarding configuration of the guild. Returns a 200 with the Onboarding object for the guild.
Requires the MANAGE_GUILD and MANAGE_ROLES permissions.

<LuaParamTable params={[ { name: "json", type: "builders.onboardingJSON" }, { name: "reason", type: "string?" } ]} />

### getGuildAuditLogAsync

> <LuaDeclaration name="getGuildAuditLogAsync" className="Guild" args={[ { name: "userId", type: "apiTypes.Snowflake?" }, { name: "actionType", type: "apiTypes.ActionTypes?" }, { name: "before", type: "apiTypes.Snowflake?" }, { name: "after", type: "apiTypes.Snowflake?" }, { name: "limit", type: "number?" } ]} isMethod />

Returns an audit log object for the guild. Requires the VIEW_AUDIT_LOG permission.

The returned list of audit log entries is ordered based on whether you use before or after. When using before, the
	list is ordered by the audit log entry ID descending (newer entries first). If after is used, the list is reversed
	and appears in ascending order (older entries first). Omitting both before and after defaults to before the current
	timestamp and will show the most recent entries in descending order by ID, the opposite can be achieved using after=0
	(showing oldest entries).

<LuaParamTable params={[ { name: "userId", type: "apiTypes.Snowflake?" }, { name: "actionType", type: "apiTypes.ActionTypes?" }, { name: "before", type: "apiTypes.Snowflake?" }, { name: "after", type: "apiTypes.Snowflake?" }, { name: "limit", type: "number?" } ]} />

### listAutomoderationRulesAsync

> <LuaDeclaration name="listAutomoderationRulesAsync" className="Guild" args={[  ]} isMethod />

Get a list of all rules currently configured for the guild. Returns a list of auto moderation rule objects for the given guild.

### getAutomoderationRuleAsync

> <LuaDeclaration name="getAutomoderationRuleAsync" className="Guild" args={[ { name: "ruleId", type: "apiTypes.Snowflake" } ]} isMethod />

Get a single rule. Returns an auto moderation rule object.

<LuaParamTable params={[ { name: "ruleId", type: "apiTypes.Snowflake" } ]} />

### createAutomoderationRuleAsync

> <LuaDeclaration name="createAutomoderationRuleAsync" className="Guild" args={[ { name: "ruleJSON", type: "builders.AutomoderationRuleJSON" }, { name: "reason", type: "string?" } ]} isMethod />

Create a new rule. Returns an auto moderation rule on success.

<LuaParamTable params={[ { name: "ruleJSON", type: "builders.AutomoderationRuleJSON" }, { name: "reason", type: "string?" } ]} />


