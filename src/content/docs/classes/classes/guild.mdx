---
title: Guild
description: DiscordLuau docs for Guild.
sidebar:
  order: 100
---

[//]: # (This file was @generated from moonwave comments. Do not edit by hand.)
[//]: # (To edit docs, change the source comments in the DiscordLuau repo.)

import LuaDeclaration from '$/components/LuaDeclaration.astro';
import LuaProperty from '$/components/LuaProperty.astro';
import LuaSummary from '$/components/LuaSummary.astro';
import LuaParamTable from '$/components/LuaParamTable.astro';

Implementation of the Discord Guild class in Luau.
Represents a Discord guild (server) with all its properties and methods.

https://discord.com/developers/docs/resources/guild#guild-object

<LuaSummary className="Guild" properties={[ { name: "stickers", type: "{Classes.Sticker}" }, { name: "roles", type: "{Classes.Guild.Role}" }, { name: "emojis", type: "{Classes.Emoji}" }, { name: "verificationLevel", type: "string" }, { name: "defaultMessageNotifications", type: "string" }, { name: "explicitContentFilter", type: "string" }, { name: "premiumTier", type: "string" }, { name: "nsfwLevel", type: "string" }, { name: "mfaLevel", type: "string" }, { name: "systemChannelFlags", type: "Classes.SystemChannelBitflag" }, { name: "icon", type: "string?" }, { name: "iconHash", type: "string?" }, { name: "name", type: "string?" }, { name: "splash", type: "string?" }, { name: "discoverySplash", type: "string?" }, { name: "owner", type: "boolean?" }, { name: "ownerId", type: "string?" }, { name: "permissions", type: "string?" }, { name: "region", type: "string?" }, { name: "afkChannelId", type: "string?" }, { name: "afkTimeout", type: "number?" }, { name: "widgetEnabled", type: "boolean?" }, { name: "widgetChannelId", type: "string?" }, { name: "features", type: "{string}?" }, { name: "applicationId", type: "string?" }, { name: "systemChannelId", type: "string?" }, { name: "rulesChannelId", type: "string?" }, { name: "maxPresences", type: "number?" }, { name: "maxMembers", type: "number?" }, { name: "vanityUrlCode", type: "string?" }, { name: "description", type: "string?" }, { name: "banner", type: "string?" }, { name: "premiumSubscriptionCount", type: "number?" }, { name: "preferredLocale", type: "string?" }, { name: "publicUpdatesChannelId", type: "string?" }, { name: "maxVideoChannelUsers", type: "number?" }, { name: "maxStageVideoChannelUsers", type: "number?" }, { name: "approximateMemberCount", type: "number?" }, { name: "approximatePresenceCount", type: "number?" }, { name: "premiumProgressBarEnabled", type: "boolean?" }, { name: "safetyAlertsChannelId", type: "string?" } ]} methods={[ { name: "createSlashCommandAsync", isMethod: true, args: [ { name: "slashCommand", type: "builders.interactionJSON" } ], returnType: "future.FutureLike<string, commandObject.Command>" }, { name: "deleteSlashCommandAsync", isMethod: true, args: [ { name: "slashCommandId", type: "apiTypes.Snowflake" } ], returnType: "future.FutureLike<string, nil>" }, { name: "editSlashCommandAsync", isMethod: true, args: [ { name: "slashCommandId", type: "apiTypes.Snowflake" }, { name: "slashCommand", type: "builders.interactionJSON" } ], returnType: "future.FutureLike<string, commandObject.Command>" }, { name: "getSlashCommandsAsync", isMethod: true, args: [ { name: "withLocalizations", type: "boolean?" } ], returnType: "future.FutureLike<string, { commandObject.Command }>" }, { name: "overwriteSlashCommandsAsync", isMethod: true, args: [ { name: "slashCommands", type: "{ builders.interactionJSON }" } ], returnType: "future.FutureLike<string, { commandObject.Command }>" }, { name: "getPreviewAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, preview.Preview>" }, { name: "modifyAsync", isMethod: true, args: [ { name: "json", type: "builders.guildJSON" }, { name: "reason", type: "string?" } ], returnType: "future.FutureLike<string, Guild>" }, { name: "deleteAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, nil>" }, { name: "getChannelsAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, { apiTypes.AbstractChannel }>" }, { name: "getTextChannelsAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, { guildText.GuildText }>" }, { name: "getVoiceChannelsAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, { guildVoice.GuildVoice }>" }, { name: "getCategoryChannelsAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, { guildCategory.GuildCategory }>" }, { name: "getAnnouncementChannelsAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, { guildAnnouncement.GuildAnnouncement }>" }, { name: "getStageVoiceChannelsAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, { guildStageVoice.GuildStageVoice }>" }, { name: "getDirectoryChannelsAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, { guildDirectory.GuildDirectory }>" }, { name: "createChannelAsync", isMethod: true, args: [ { name: "json", type: "builders.channelJSON" }, { name: "reason", type: "string?" } ], returnType: "future.FutureLike<string, { apiTypes.AbstractChannel }>" }, { name: "getRoleMemberCounts", isMethod: true, args: [  ], returnType: "future.FutureLike<string, { [string]: number }>" }, { name: "getThreadsAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, { publicThread.Public | privateThread.Private }>" }, { name: "getAnnouncementThreadsAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, { announcementThread.Announcement }>" }, { name: "getPublicThreadsAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, { publicThread.Public }>" }, { name: "getPrivateThreadsAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, { privateThread.Private }>" }, { name: "getMemberAsync", isMethod: true, args: [ { name: "userId", type: "string" } ], returnType: "future.FutureLike<string, member.Member>" }, { name: "listMembersAsync", isMethod: true, args: [ { name: "lastSnowflake", type: "string?" }, { name: "limit", type: "number?" } ], returnType: "future.FutureLike<string, { member.Member }>" }, { name: "searchMembersAsync", isMethod: true, args: [ { name: "query", type: "string" }, { name: "limit", type: "number?" } ], returnType: "future.FutureLike<string, { member.Member }>" }, { name: "addMemberAsync", isMethod: true, args: [ { name: "userId", type: "apiTypes.Snowflake" }, { name: "accessToken", type: "string" }, { name: "nickname", type: "string?" }, { name: "roles", type: "{ apiTypes.Snowflake }?" }, { name: "muted", type: "boolean?" }, { name: "deafened", type: "boolean?" } ], returnType: "future.FutureLike<string, member.Member?>" }, { name: "kickMemberAsync", isMethod: true, args: [ { name: "userId", type: "apiTypes.Snowflake" }, { name: "reason", type: "string?" } ], returnType: "future.FutureLike<string, nil>" }, { name: "banMemberAsync", isMethod: true, args: [ { name: "userId", type: "apiTypes.Snowflake" }, { name: "reason", type: "string?" }, { name: "deleteMessageDays", type: "number?" }, { name: "deleteMessageSeconds", type: "number?" } ], returnType: "future.FutureLike<string, nil>" }, { name: "bulkBanAsync", isMethod: true, args: [ { name: "userIds", type: "{ apiTypes.Snowflake }" }, { name: "reason", type: "string?" }, { name: "deleteMessageSeconds", type: "number?" } ], returnType: "future.Future< string, { failedUsers: { apiTypes.Snowflake }, bannedUsers: { apiTypes.Snowflake } } >" }, { name: "getBansAsync", isMethod: true, args: [ { name: "limit", type: "number?" }, { name: "before", type: "apiTypes.Snowflake?" }, { name: "after", type: "apiTypes.Snowflake?" } ], returnType: "future.FutureLike<string, { ban.Ban }>" }, { name: "getBanAsync", isMethod: true, args: [ { name: "userId", type: "apiTypes.Snowflake" } ], returnType: "future.FutureLike<string, ban.Ban?>" }, { name: "getRolesAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, { role.Role }>" }, { name: "getRoleAsync", isMethod: true, args: [ { name: "roleId", type: "apiTypes.Snowflake" } ], returnType: "future.FutureLike<string, role.Role>" }, { name: "createRoleAsync", isMethod: true, args: [ { name: "roleJson", type: "builders.roleJSON" }, { name: "reason", type: "string?" } ], returnType: "future.FutureLike<string, role.Role>" }, { name: "setMFALevelAsync", isMethod: true, args: [ { name: "mfaLevel", type: "apiTypes.MFALevel" }, { name: "reason", type: "string?" } ], returnType: "future.FutureLike<string, apiTypes.MFALevel>" }, { name: "getPruneCountAsync", isMethod: true, args: [ { name: "days", type: "number?" }, { name: "includedRoles", type: "{ apiTypes.Snowflake }?" } ], returnType: "future.FutureLike<string, number>" }, { name: "beginPruneAsync", isMethod: true, args: [ { name: "days", type: "number?" }, { name: "returnPruneCount", type: "boolean?" }, { name: "includedRoles", type: "{ apiTypes.Snowflake }?" }, { name: "reason", type: "string?" } ], returnType: "future.FutureLike<string, number>" }, { name: "getVoiceRegionsAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, { voiceRegion.VoiceRegion }>" }, { name: "getInvitesAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, { invite.Invite }>" }, { name: "getIntegrationsAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, { integration.Integration }>" }, { name: "deleteIntegrationAsync", isMethod: true, args: [ { name: "integrationId", type: "string" } ], returnType: "future.FutureLike<string, nil>" }, { name: "getWidgetSettingsAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, widgetSettings.WidgetSettings>" }, { name: "modifyWidgetAsync", isMethod: true, args: [ { name: "json", type: "builders.widgetSettingsJSON" }, { name: "reason", type: "string?" } ], returnType: "future.FutureLike<string, widgetSettings.WidgetSettings>" }, { name: "getWidgetAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, widget.Widget>" }, { name: "getVanityUrlAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, vanityUrl.VanityUrl>" }, { name: "getWidgetImageAsync", isMethod: true, args: [ { name: "style", type: "&quot;shield&quot; | &quot;banner1&quot; | &quot;banner2&quot; | &quot;banner3&quot; | &quot;banner4&quot;" } ], returnType: "future.FutureLike<string, string>" }, { name: "getWelcomeScreenAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, welcomeScreen.WelcomeScreen>" }, { name: "modifyWelcomeScreenAsync", isMethod: true, args: [ { name: "json", type: "builders.welcomeScreenJSON" }, { name: "reason", type: "string?" } ], returnType: "future.FutureLike<string, welcomeScreen.WelcomeScreen>" }, { name: "getOnboardingAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, onboarding.Onboarding>" }, { name: "modifyOnboardingAsync", isMethod: true, args: [ { name: "json", type: "builders.onboardingJSON" }, { name: "reason", type: "string?" } ], returnType: "future.FutureLike<string, onboarding.Onboarding>" }, { name: "getGuildAuditLogAsync", isMethod: true, args: [ { name: "userId", type: "apiTypes.Snowflake?" }, { name: "actionType", type: "apiTypes.ActionTypes?" }, { name: "before", type: "apiTypes.Snowflake?" }, { name: "after", type: "apiTypes.Snowflake?" }, { name: "limit", type: "number?" } ], returnType: "future.FutureLike<string, auditlog.AuditLogs>" }, { name: "listAutomoderationRulesAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, { automoderationRule.AutomoderationRule }>" }, { name: "getAutomoderationRuleAsync", isMethod: true, args: [ { name: "ruleId", type: "apiTypes.Snowflake" } ], returnType: "future.FutureLike<string, automoderationRule.AutomoderationRule>" }, { name: "createAutomoderationRuleAsync", isMethod: true, args: [ { name: "ruleJSON", type: "builders.AutomoderationRuleJSON" }, { name: "reason", type: "string?" } ], returnType: "future.FutureLike<string, automoderationRule.AutomoderationRule>" } ]} />

## Properties

### stickers

<LuaProperty name="Guild.stickers" type="{Classes.Sticker}" />

---
Collection of stickers in the guild

### roles

<LuaProperty name="Guild.roles" type="{Classes.Guild.Role}" />

---
Collection of roles in the guild

### emojis

<LuaProperty name="Guild.emojis" type="{Classes.Emoji}" />

---
Collection of emojis in the guild

### verificationLevel

<LuaProperty name="Guild.verificationLevel" type="string" />

---
Verification level required for the guild

### defaultMessageNotifications

<LuaProperty name="Guild.defaultMessageNotifications" type="string" />

---
Default message notifications level

### explicitContentFilter

<LuaProperty name="Guild.explicitContentFilter" type="string" />

---
Explicit content filter level

### premiumTier

<LuaProperty name="Guild.premiumTier" type="string" />

---
Premium tier (Server Boost level)

### nsfwLevel

<LuaProperty name="Guild.nsfwLevel" type="string" />

---
NSFW level of the guild

### mfaLevel

<LuaProperty name="Guild.mfaLevel" type="string" />

---
Required MFA level for the guild

### systemChannelFlags

<LuaProperty name="Guild.systemChannelFlags" type="Classes.SystemChannelBitflag" />

---
System channel flags

### icon

<LuaProperty name="Guild.icon" type="string?" />

---
Icon hash of the guild

### iconHash

<LuaProperty name="Guild.iconHash" type="string?" />

---
The hash of the guild's icon

### name

<LuaProperty name="Guild.name" type="string?" />

---
The name of the guild

### splash

<LuaProperty name="Guild.splash" type="string?" />

---
Splash hash

### discoverySplash

<LuaProperty name="Guild.discoverySplash" type="string?" />

---
Discovery splash hash

### owner

<LuaProperty name="Guild.owner" type="boolean?" />

---
Whether the user is the owner of the guild

### ownerId

<LuaProperty name="Guild.ownerId" type="string?" />

---
ID of guild owner

### permissions

<LuaProperty name="Guild.permissions" type="string?" />

---
Total permissions for the user in the guild (excludes overwrites)

### region

<LuaProperty name="Guild.region" type="string?" />

---
Voice region id for the guild

### afkChannelId

<LuaProperty name="Guild.afkChannelId" type="string?" />

---
ID of AFK channel

### afkTimeout

<LuaProperty name="Guild.afkTimeout" type="number?" />

---
AFK timeout in seconds

### widgetEnabled

<LuaProperty name="Guild.widgetEnabled" type="boolean?" />

---
Whether the server widget is enabled

### widgetChannelId

<LuaProperty name="Guild.widgetChannelId" type="string?" />

---
The channel id that the widget will generate an invite to

### features

<LuaProperty name="Guild.features" type="{string}?" />

---
Array of guild features

### applicationId

<LuaProperty name="Guild.applicationId" type="string?" />

---
Application ID of the guild creator if it is bot-created

### systemChannelId

<LuaProperty name="Guild.systemChannelId" type="string?" />

---
The id of the channel where guild notices are posted

### rulesChannelId

<LuaProperty name="Guild.rulesChannelId" type="string?" />

---
ID of the rules channel

### maxPresences

<LuaProperty name="Guild.maxPresences" type="number?" />

---
The maximum number of presences for the guild

### maxMembers

<LuaProperty name="Guild.maxMembers" type="number?" />

---
The maximum number of members for the guild

### vanityUrlCode

<LuaProperty name="Guild.vanityUrlCode" type="string?" />

---
The vanity URL code for the guild

### description

<LuaProperty name="Guild.description" type="string?" />

---
The description of the guild

### banner

<LuaProperty name="Guild.banner" type="string?" />

---
Banner hash

### premiumSubscriptionCount

<LuaProperty name="Guild.premiumSubscriptionCount" type="number?" />

---
The number of boosts this guild currently has

### preferredLocale

<LuaProperty name="Guild.preferredLocale" type="string?" />

---
The preferred locale of a Community guild

### publicUpdatesChannelId

<LuaProperty name="Guild.publicUpdatesChannelId" type="string?" />

---
The id of the channel where admins and moderators receive notices from Discord

### maxVideoChannelUsers

<LuaProperty name="Guild.maxVideoChannelUsers" type="number?" />

---
The maximum amount of users in a video channel

### maxStageVideoChannelUsers

<LuaProperty name="Guild.maxStageVideoChannelUsers" type="number?" />

---
The maximum amount of users in a stage video channel

### approximateMemberCount

<LuaProperty name="Guild.approximateMemberCount" type="number?" />

---
Approximate number of members in this guild

### approximatePresenceCount

<LuaProperty name="Guild.approximatePresenceCount" type="number?" />

---
Approximate number of non-offline members in this guild

### premiumProgressBarEnabled

<LuaProperty name="Guild.premiumProgressBarEnabled" type="boolean?" />

---
Whether the guild has the boost progress bar enabled

### safetyAlertsChannelId

<LuaProperty name="Guild.safetyAlertsChannelId" type="string?" />

---
The id of the channel where admins and moderators receive safety alerts from Discord


## Methods

### createSlashCommandAsync

> <LuaDeclaration name="createSlashCommandAsync" className="Guild" args={[ { name: "slashCommand", type: "builders.interactionJSON" } ]} returnType="future.FutureLike<string, commandObject.Command>" isMethod />

Creates a guild Slash command for the application.

<LuaParamTable params={[ { name: "slashCommand", type: "builders.interactionJSON" } ]} returns={[ { type: "future.FutureLike<string, commandObject.Command>" } ]} />

### deleteSlashCommandAsync

> <LuaDeclaration name="deleteSlashCommandAsync" className="Guild" args={[ { name: "slashCommandId", type: "apiTypes.Snowflake" } ]} returnType="future.FutureLike<string, nil>" isMethod />

Delete an existing guild application command.

<LuaParamTable params={[ { name: "slashCommandId", type: "apiTypes.Snowflake" } ]} returns={[ { type: "future.FutureLike<string, nil>" } ]} />

### editSlashCommandAsync

> <LuaDeclaration name="editSlashCommandAsync" className="Guild" args={[ { name: "slashCommandId", type: "apiTypes.Snowflake" }, { name: "slashCommand", type: "builders.interactionJSON" } ]} returnType="future.FutureLike<string, commandObject.Command>" isMethod />

Edit an existing guild application command.

<LuaParamTable params={[ { name: "slashCommandId", type: "apiTypes.Snowflake" }, { name: "slashCommand", type: "builders.interactionJSON" } ]} returns={[ { type: "future.FutureLike<string, commandObject.Command>" } ]} />

### getSlashCommandsAsync

> <LuaDeclaration name="getSlashCommandsAsync" className="Guild" args={[ { name: "withLocalizations", type: "boolean?" } ]} returnType="future.FutureLike<string, { commandObject.Command }>" isMethod />

Get a list of guild application commands.

<LuaParamTable params={[ { name: "withLocalizations", type: "boolean?" } ]} returns={[ { type: "future.FutureLike<string, { commandObject.Command }>" } ]} />

### overwriteSlashCommandsAsync

> <LuaDeclaration name="overwriteSlashCommandsAsync" className="Guild" args={[ { name: "slashCommands", type: "{ builders.interactionJSON }" } ]} returnType="future.FutureLike<string, { commandObject.Command }>" isMethod />

Overwrite all guild application commands. This will skip over commands that are the same as the current commands,
and will not remove any existing commands.

<LuaParamTable params={[ { name: "slashCommands", type: "{ builders.interactionJSON }" } ]} returns={[ { type: "future.FutureLike<string, { commandObject.Command }>" } ]} />

### getPreviewAsync

> <LuaDeclaration name="getPreviewAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, preview.Preview>" isMethod />

Returns the guild preview object for the given id. If the user is not in the guild, then the guild must be
discoverable.

<LuaParamTable returns={[ { type: "future.FutureLike<string, preview.Preview>" } ]} />

### modifyAsync

> <LuaDeclaration name="modifyAsync" className="Guild" args={[ { name: "json", type: "builders.guildJSON" }, { name: "reason", type: "string?" } ]} returnType="future.FutureLike<string, Guild>" isMethod />

Modify a guild's settings. Requires the MANAGE_GUILD permission.
Returns the updated guild object on success. Fires a Guild Update Gateway event.

<LuaParamTable params={[ { name: "json", type: "builders.guildJSON" }, { name: "reason", type: "string?" } ]} returns={[ { type: "future.FutureLike<string, Guild>" } ]} />

### deleteAsync

> <LuaDeclaration name="deleteAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, nil>" isMethod />

Delete a guild permanently. User must be owner.
Returns 204 No Content on success. Fires a Guild Delete Gateway event.

<LuaParamTable returns={[ { type: "future.FutureLike<string, nil>" } ]} />

### getChannelsAsync

> <LuaDeclaration name="getChannelsAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, { apiTypes.AbstractChannel }>" isMethod />

Returns a list of guild channel objects. Does not include threads.

Typed as unknown because the API returns a variety of channel types, and luau's type system is not currently
capable of handling this.

Please use explicit functions for specific channel types, for example: getTextChannelsAsync, getVoiceChannelsAsync, etc.

<LuaParamTable returns={[ { type: "future.FutureLike<string, { apiTypes.AbstractChannel }>" } ]} />

### getTextChannelsAsync

> <LuaDeclaration name="getTextChannelsAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, { guildText.GuildText }>" isMethod />

Returns a list of guild text channel objects.

<LuaParamTable returns={[ { type: "future.FutureLike<string, { guildText.GuildText }>" } ]} />

### getVoiceChannelsAsync

> <LuaDeclaration name="getVoiceChannelsAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, { guildVoice.GuildVoice }>" isMethod />

Returns a list of guild voice channel objects.

<LuaParamTable returns={[ { type: "future.FutureLike<string, { guildVoice.GuildVoice }>" } ]} />

### getCategoryChannelsAsync

> <LuaDeclaration name="getCategoryChannelsAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, { guildCategory.GuildCategory }>" isMethod />

Returns a list of guild category channel objects.

<LuaParamTable returns={[ { type: "future.FutureLike<string, { guildCategory.GuildCategory }>" } ]} />

### getAnnouncementChannelsAsync

> <LuaDeclaration name="getAnnouncementChannelsAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, { guildAnnouncement.GuildAnnouncement }>" isMethod />

Returns a list of guild announcement channel objects.

<LuaParamTable returns={[ { type: "future.FutureLike<string, { guildAnnouncement.GuildAnnouncement }>" } ]} />

### getStageVoiceChannelsAsync

> <LuaDeclaration name="getStageVoiceChannelsAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, { guildStageVoice.GuildStageVoice }>" isMethod />

Returns a list of guild stage voice channel objects.

<LuaParamTable returns={[ { type: "future.FutureLike<string, { guildStageVoice.GuildStageVoice }>" } ]} />

### getDirectoryChannelsAsync

> <LuaDeclaration name="getDirectoryChannelsAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, { guildDirectory.GuildDirectory }>" isMethod />

Returns a list of guild directory channel objects.

<LuaParamTable returns={[ { type: "future.FutureLike<string, { guildDirectory.GuildDirectory }>" } ]} />

### createChannelAsync

> <LuaDeclaration name="createChannelAsync" className="Guild" args={[ { name: "json", type: "builders.channelJSON" }, { name: "reason", type: "string?" } ]} returnType="future.FutureLike<string, { apiTypes.AbstractChannel }>" isMethod />

Create a new channel object for the guild. Requires the MANAGE_CHANNELS permission.
If setting permission overwrites, only permissions your bot has in the guild can be allowed/denied.
Setting MANAGE_ROLES permission in channels is only possible for guild administrators.
Returns the new channel object on success. Fires a Channel Create Gateway event.

<LuaParamTable params={[ { name: "json", type: "builders.channelJSON" }, { name: "reason", type: "string?" } ]} returns={[ { type: "future.FutureLike<string, { apiTypes.AbstractChannel }>" } ]} />

### getRoleMemberCounts

> <LuaDeclaration name="getRoleMemberCounts" className="Guild" args={[  ]} returnType="future.FutureLike<string, { [string]: number }>" isMethod />

Returns a map of role IDs to the number of members with the role. Does not include the @everyone role.

<LuaParamTable returns={[ { type: "future.FutureLike<string, { [string]: number }>" } ]} />

### getThreadsAsync

> <LuaDeclaration name="getThreadsAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, { publicThread.Public | privateThread.Private }>" isMethod />

Returns all active threads in the guild, including public and private threads.
Threads are ordered by their id, in descending order.

Typed as unknown because the API returns a variety of channel types, and luau's type system is not currently
capable of handling this.

Please use explicit functions for specific channel types, for example: getMediaThreadsAsync, getAnnouncementThreadsAsync, etc.

<LuaParamTable returns={[ { type: "future.FutureLike<string, { publicThread.Public | privateThread.Private }>" } ]} />

### getAnnouncementThreadsAsync

> <LuaDeclaration name="getAnnouncementThreadsAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, { announcementThread.Announcement }>" isMethod />

Returns a list of guild announcement thread objects.

<LuaParamTable returns={[ { type: "future.FutureLike<string, { announcementThread.Announcement }>" } ]} />

### getPublicThreadsAsync

> <LuaDeclaration name="getPublicThreadsAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, { publicThread.Public }>" isMethod />

Returns a list of public guild thread objects.

<LuaParamTable returns={[ { type: "future.FutureLike<string, { publicThread.Public }>" } ]} />

### getPrivateThreadsAsync

> <LuaDeclaration name="getPrivateThreadsAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, { privateThread.Private }>" isMethod />

Returns a list of private guild thread objects.

<LuaParamTable returns={[ { type: "future.FutureLike<string, { privateThread.Private }>" } ]} />

### getMemberAsync

> <LuaDeclaration name="getMemberAsync" className="Guild" args={[ { name: "userId", type: "string" } ]} returnType="future.FutureLike<string, member.Member>" isMethod />

Returns a guild member object for the specified user.

<LuaParamTable params={[ { name: "userId", type: "string" } ]} returns={[ { type: "future.FutureLike<string, member.Member>" } ]} />

### listMembersAsync

> <LuaDeclaration name="listMembersAsync" className="Guild" args={[ { name: "lastSnowflake", type: "string?" }, { name: "limit", type: "number?" } ]} returnType="future.FutureLike<string, { member.Member }>" isMethod />

Returns a list of guild member objects that are members of the guild.

<LuaParamTable params={[ { name: "lastSnowflake", type: "string?" }, { name: "limit", type: "number?" } ]} returns={[ { type: "future.FutureLike<string, { member.Member }>" } ]} />

### searchMembersAsync

> <LuaDeclaration name="searchMembersAsync" className="Guild" args={[ { name: "query", type: "string" }, { name: "limit", type: "number?" } ]} returnType="future.FutureLike<string, { member.Member }>" isMethod />

Returns a list of guild member objects whose username or nickname starts with a provided string.

<LuaParamTable params={[ { name: "query", type: "string" }, { name: "limit", type: "number?" } ]} returns={[ { type: "future.FutureLike<string, { member.Member }>" } ]} />

### addMemberAsync

> <LuaDeclaration name="addMemberAsync" className="Guild" args={[ { name: "userId", type: "apiTypes.Snowflake" }, { name: "accessToken", type: "string" }, { name: "nickname", type: "string?" }, { name: "roles", type: "{ apiTypes.Snowflake }?" }, { name: "muted", type: "boolean?" }, { name: "deafened", type: "boolean?" } ]} returnType="future.FutureLike<string, member.Member?>" isMethod />

Adds a user to the guild, provided you have a valid oauth2 access token for the user with the guilds.join scope.

For guilds with Membership Screening enabled, this endpoint will default to adding new members as pending in the
guild member object. Members that are pending will have to complete membership screening before they become full
members that can talk.

<LuaParamTable params={[ { name: "userId", type: "apiTypes.Snowflake" }, { name: "accessToken", type: "string" }, { name: "nickname", type: "string?" }, { name: "roles", type: "{ apiTypes.Snowflake }?" }, { name: "muted", type: "boolean?" }, { name: "deafened", type: "boolean?" } ]} returns={[ { type: "future.FutureLike<string, member.Member?>" } ]} />

### kickMemberAsync

> <LuaDeclaration name="kickMemberAsync" className="Guild" args={[ { name: "userId", type: "apiTypes.Snowflake" }, { name: "reason", type: "string?" } ]} returnType="future.FutureLike<string, nil>" isMethod />

Removes a role from a guild member. Requires the MANAGE_ROLES permission.

<LuaParamTable params={[ { name: "userId", type: "apiTypes.Snowflake" }, { name: "reason", type: "string?" } ]} returns={[ { type: "future.FutureLike<string, nil>" } ]} />

### banMemberAsync

> <LuaDeclaration name="banMemberAsync" className="Guild" args={[ { name: "userId", type: "apiTypes.Snowflake" }, { name: "reason", type: "string?" }, { name: "deleteMessageDays", type: "number?" }, { name: "deleteMessageSeconds", type: "number?" } ]} returnType="future.FutureLike<string, nil>" isMethod />

Create a guild ban, and optionally delete previous messages sent by the banned user.

<LuaParamTable params={[ { name: "userId", type: "apiTypes.Snowflake" }, { name: "reason", type: "string?" }, { name: "deleteMessageDays", type: "number?" }, { name: "deleteMessageSeconds", type: "number?" } ]} returns={[ { type: "future.FutureLike<string, nil>" } ]} />

### bulkBanAsync

> <LuaDeclaration name="bulkBanAsync" className="Guild" args={[ { name: "userIds", type: "{ apiTypes.Snowflake }" }, { name: "reason", type: "string?" }, { name: "deleteMessageSeconds", type: "number?" } ]} returnType="future.Future< string, { failedUsers: { apiTypes.Snowflake }, bannedUsers: { apiTypes.Snowflake } } >" isMethod />

Create a guild ban for multiple users, and optionally delete previous messages sent by the banned users.

<LuaParamTable params={[ { name: "userIds", type: "{ apiTypes.Snowflake }" }, { name: "reason", type: "string?" }, { name: "deleteMessageSeconds", type: "number?" } ]} returns={[ { type: "future.Future< string, { failedUsers: { apiTypes.Snowflake }, bannedUsers: { apiTypes.Snowflake } } >" } ]} />

### getBansAsync

> <LuaDeclaration name="getBansAsync" className="Guild" args={[ { name: "limit", type: "number?" }, { name: "before", type: "apiTypes.Snowflake?" }, { name: "after", type: "apiTypes.Snowflake?" } ]} returnType="future.FutureLike<string, { ban.Ban }>" isMethod />

Returns a list of ban objects for the users banned from this guild.

<LuaParamTable params={[ { name: "limit", type: "number?" }, { name: "before", type: "apiTypes.Snowflake?" }, { name: "after", type: "apiTypes.Snowflake?" } ]} returns={[ { type: "future.FutureLike<string, { ban.Ban }>" } ]} />

### getBanAsync

> <LuaDeclaration name="getBanAsync" className="Guild" args={[ { name: "userId", type: "apiTypes.Snowflake" } ]} returnType="future.FutureLike<string, ban.Ban?>" isMethod />

Returns a ban object for the given user or a 404 not found if the ban cannot be found.

<LuaParamTable params={[ { name: "userId", type: "apiTypes.Snowflake" } ]} returns={[ { type: "future.FutureLike<string, ban.Ban?>" } ]} />

### getRolesAsync

> <LuaDeclaration name="getRolesAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, { role.Role }>" isMethod />

Returns a list of role objects for the guild.

<LuaParamTable returns={[ { type: "future.FutureLike<string, { role.Role }>" } ]} />

### getRoleAsync

> <LuaDeclaration name="getRoleAsync" className="Guild" args={[ { name: "roleId", type: "apiTypes.Snowflake" } ]} returnType="future.FutureLike<string, role.Role>" isMethod />

Returns a role object for the specified role.

<LuaParamTable params={[ { name: "roleId", type: "apiTypes.Snowflake" } ]} returns={[ { type: "future.FutureLike<string, role.Role>" } ]} />

### createRoleAsync

> <LuaDeclaration name="createRoleAsync" className="Guild" args={[ { name: "roleJson", type: "builders.roleJSON" }, { name: "reason", type: "string?" } ]} returnType="future.FutureLike<string, role.Role>" isMethod />

Create a new role for the guild. Requires the MANAGE_ROLES permission.
Returns the new role object on success.

<LuaParamTable params={[ { name: "roleJson", type: "builders.roleJSON" }, { name: "reason", type: "string?" } ]} returns={[ { type: "future.FutureLike<string, role.Role>" } ]} />

### setMFALevelAsync

> <LuaDeclaration name="setMFALevelAsync" className="Guild" args={[ { name: "mfaLevel", type: "apiTypes.MFALevel" }, { name: "reason", type: "string?" } ]} returnType="future.FutureLike<string, apiTypes.MFALevel>" isMethod />

Modify a guild's MFA level. Requires guild ownership. Returns the updated level on success.

<LuaParamTable params={[ { name: "mfaLevel", type: "apiTypes.MFALevel" }, { name: "reason", type: "string?" } ]} returns={[ { type: "future.FutureLike<string, apiTypes.MFALevel>" } ]} />

### getPruneCountAsync

> <LuaDeclaration name="getPruneCountAsync" className="Guild" args={[ { name: "days", type: "number?" }, { name: "includedRoles", type: "{ apiTypes.Snowflake }?" } ]} returnType="future.FutureLike<string, number>" isMethod />

Returns an object with one pruned key indicating the number of members that would be removed in a prune operation.
Requires the MANAGE_GUILD and KICK_MEMBERS permissions.

By default, prune will not remove users with roles. You can optionally include specific roles in your prune by
providing the include_roles parameter. Any inactive user that has a subset of the provided role(s) will be
counted in the prune and users with additional roles will not.

<LuaParamTable params={[ { name: "days", type: "number?" }, { name: "includedRoles", type: "{ apiTypes.Snowflake }?" } ]} returns={[ { type: "future.FutureLike<string, number>" } ]} />

### beginPruneAsync

> <LuaDeclaration name="beginPruneAsync" className="Guild" args={[ { name: "days", type: "number?" }, { name: "returnPruneCount", type: "boolean?" }, { name: "includedRoles", type: "{ apiTypes.Snowflake }?" }, { name: "reason", type: "string?" } ]} returnType="future.FutureLike<string, number>" isMethod />

Begin a prune operation. Requires the MANAGE_GUILD and KICK_MEMBERS permissions.
Returns an object with one pruned key indicating the number of members that were removed in the prune operation.

For large guilds it's recommended to set the compute_prune_count option to false, forcing pruned to null

By default, prune will not remove users with roles. You can optionally include specific roles in your prune by
providing the include_roles parameter. Any inactive user that has a subset of the provided role(s)
will be included in the prune and users with additional roles will not.

<LuaParamTable params={[ { name: "days", type: "number?" }, { name: "returnPruneCount", type: "boolean?" }, { name: "includedRoles", type: "{ apiTypes.Snowflake }?" }, { name: "reason", type: "string?" } ]} returns={[ { type: "future.FutureLike<string, number>" } ]} />

### getVoiceRegionsAsync

> <LuaDeclaration name="getVoiceRegionsAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, { voiceRegion.VoiceRegion }>" isMethod />

Returns an array of voice region objects that can be used when setting a voice or stage channel's rtc_region.

<LuaParamTable returns={[ { type: "future.FutureLike<string, { voiceRegion.VoiceRegion }>" } ]} />

### getInvitesAsync

> <LuaDeclaration name="getInvitesAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, { invite.Invite }>" isMethod />

Returns a list of invite objects (with invite metadata) for the guild. Requires the MANAGE_GUILD permission.

<LuaParamTable returns={[ { type: "future.FutureLike<string, { invite.Invite }>" } ]} />

### getIntegrationsAsync

> <LuaDeclaration name="getIntegrationsAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, { integration.Integration }>" isMethod />

Returns a list of integration objects for the guild. Requires the MANAGE_GUILD permission.

<LuaParamTable returns={[ { type: "future.FutureLike<string, { integration.Integration }>" } ]} />

### deleteIntegrationAsync

> <LuaDeclaration name="deleteIntegrationAsync" className="Guild" args={[ { name: "integrationId", type: "string" } ]} returnType="future.FutureLike<string, nil>" isMethod />

Delete the attached integration object for the guild. Deletes any associated webhooks and kicks the associated
bot if there is one.

Requires the MANAGE_GUILD permission.

<LuaParamTable params={[ { name: "integrationId", type: "string" } ]} returns={[ { type: "future.FutureLike<string, nil>" } ]} />

### getWidgetSettingsAsync

> <LuaDeclaration name="getWidgetSettingsAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, widgetSettings.WidgetSettings>" isMethod />

Returns a guild widget settings object. Requires the MANAGE_GUILD permission.

<LuaParamTable returns={[ { type: "future.FutureLike<string, widgetSettings.WidgetSettings>" } ]} />

### modifyWidgetAsync

> <LuaDeclaration name="modifyWidgetAsync" className="Guild" args={[ { name: "json", type: "builders.widgetSettingsJSON" }, { name: "reason", type: "string?" } ]} returnType="future.FutureLike<string, widgetSettings.WidgetSettings>" isMethod />

Modify a guild widget settings object for the guild. All attributes may be passed in with JSON and modified.
Requires the MANAGE_GUILD permission. Returns the updated guild widget settings object. 

<LuaParamTable params={[ { name: "json", type: "builders.widgetSettingsJSON" }, { name: "reason", type: "string?" } ]} returns={[ { type: "future.FutureLike<string, widgetSettings.WidgetSettings>" } ]} />

### getWidgetAsync

> <LuaDeclaration name="getWidgetAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, widget.Widget>" isMethod />

Returns the widget for the guild. Fires an Invite Create Gateway event when an invite channel is defined
and a new Invite is generated.

<LuaParamTable returns={[ { type: "future.FutureLike<string, widget.Widget>" } ]} />

### getVanityUrlAsync

> <LuaDeclaration name="getVanityUrlAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, vanityUrl.VanityUrl>" isMethod />

Returns a partial invite object for guilds with that feature enabled. Requires the MANAGE_GUILD permission.
code will be null if a vanity url for the guild is not set.

<LuaParamTable returns={[ { type: "future.FutureLike<string, vanityUrl.VanityUrl>" } ]} />

### getWidgetImageAsync

> <LuaDeclaration name="getWidgetImageAsync" className="Guild" args={[ { name: "style", type: "&quot;shield&quot; | &quot;banner1&quot; | &quot;banner2&quot; | &quot;banner3&quot; | &quot;banner4&quot;" } ]} returnType="future.FutureLike<string, string>" isMethod />

Returns a PNG image widget for the guild. Requires no permissions or authentication.

<LuaParamTable params={[ { name: "style", type: "&quot;shield&quot; | &quot;banner1&quot; | &quot;banner2&quot; | &quot;banner3&quot; | &quot;banner4&quot;" } ]} returns={[ { type: "future.FutureLike<string, string>" } ]} />

### getWelcomeScreenAsync

> <LuaDeclaration name="getWelcomeScreenAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, welcomeScreen.WelcomeScreen>" isMethod />

Returns the Welcome Screen object for the guild. If the welcome screen is not enabled.

the MANAGE_GUILD permission is required.

<LuaParamTable returns={[ { type: "future.FutureLike<string, welcomeScreen.WelcomeScreen>" } ]} />

### modifyWelcomeScreenAsync

> <LuaDeclaration name="modifyWelcomeScreenAsync" className="Guild" args={[ { name: "json", type: "builders.welcomeScreenJSON" }, { name: "reason", type: "string?" } ]} returnType="future.FutureLike<string, welcomeScreen.WelcomeScreen>" isMethod />

Modify the guild's Welcome Screen. Requires the MANAGE_GUILD permission.

<LuaParamTable params={[ { name: "json", type: "builders.welcomeScreenJSON" }, { name: "reason", type: "string?" } ]} returns={[ { type: "future.FutureLike<string, welcomeScreen.WelcomeScreen>" } ]} />

### getOnboardingAsync

> <LuaDeclaration name="getOnboardingAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, onboarding.Onboarding>" isMethod />

Returns the Onboarding object for the guild.

<LuaParamTable returns={[ { type: "future.FutureLike<string, onboarding.Onboarding>" } ]} />

### modifyOnboardingAsync

> <LuaDeclaration name="modifyOnboardingAsync" className="Guild" args={[ { name: "json", type: "builders.onboardingJSON" }, { name: "reason", type: "string?" } ]} returnType="future.FutureLike<string, onboarding.Onboarding>" isMethod />

Modifies the onboarding configuration of the guild. Returns a 200 with the Onboarding object for the guild.
Requires the MANAGE_GUILD and MANAGE_ROLES permissions.

<LuaParamTable params={[ { name: "json", type: "builders.onboardingJSON" }, { name: "reason", type: "string?" } ]} returns={[ { type: "future.FutureLike<string, onboarding.Onboarding>" } ]} />

### getGuildAuditLogAsync

> <LuaDeclaration name="getGuildAuditLogAsync" className="Guild" args={[ { name: "userId", type: "apiTypes.Snowflake?" }, { name: "actionType", type: "apiTypes.ActionTypes?" }, { name: "before", type: "apiTypes.Snowflake?" }, { name: "after", type: "apiTypes.Snowflake?" }, { name: "limit", type: "number?" } ]} returnType="future.FutureLike<string, auditlog.AuditLogs>" isMethod />

Returns an audit log object for the guild. Requires the VIEW_AUDIT_LOG permission.

The returned list of audit log entries is ordered based on whether you use before or after. When using before, the
	list is ordered by the audit log entry ID descending (newer entries first). If after is used, the list is reversed
	and appears in ascending order (older entries first). Omitting both before and after defaults to before the current
	timestamp and will show the most recent entries in descending order by ID, the opposite can be achieved using after=0
	(showing oldest entries).

<LuaParamTable params={[ { name: "userId", type: "apiTypes.Snowflake?" }, { name: "actionType", type: "apiTypes.ActionTypes?" }, { name: "before", type: "apiTypes.Snowflake?" }, { name: "after", type: "apiTypes.Snowflake?" }, { name: "limit", type: "number?" } ]} returns={[ { type: "future.FutureLike<string, auditlog.AuditLogs>" } ]} />

### listAutomoderationRulesAsync

> <LuaDeclaration name="listAutomoderationRulesAsync" className="Guild" args={[  ]} returnType="future.FutureLike<string, { automoderationRule.AutomoderationRule }>" isMethod />

Get a list of all rules currently configured for the guild. Returns a list of auto moderation rule objects for the given guild.

<LuaParamTable returns={[ { type: "future.FutureLike<string, { automoderationRule.AutomoderationRule }>" } ]} />

### getAutomoderationRuleAsync

> <LuaDeclaration name="getAutomoderationRuleAsync" className="Guild" args={[ { name: "ruleId", type: "apiTypes.Snowflake" } ]} returnType="future.FutureLike<string, automoderationRule.AutomoderationRule>" isMethod />

Get a single rule. Returns an auto moderation rule object.

<LuaParamTable params={[ { name: "ruleId", type: "apiTypes.Snowflake" } ]} returns={[ { type: "future.FutureLike<string, automoderationRule.AutomoderationRule>" } ]} />

### createAutomoderationRuleAsync

> <LuaDeclaration name="createAutomoderationRuleAsync" className="Guild" args={[ { name: "ruleJSON", type: "builders.AutomoderationRuleJSON" }, { name: "reason", type: "string?" } ]} returnType="future.FutureLike<string, automoderationRule.AutomoderationRule>" isMethod />

Create a new rule. Returns an auto moderation rule on success.

<LuaParamTable params={[ { name: "ruleJSON", type: "builders.AutomoderationRuleJSON" }, { name: "reason", type: "string?" } ]} returns={[ { type: "future.FutureLike<string, automoderationRule.AutomoderationRule>" } ]} />


