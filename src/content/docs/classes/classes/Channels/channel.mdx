---
title: Channel
description: DiscordLuau docs for Channel.
sidebar:
  order: 1
---

[//]: # (This file was @generated from moonwave comments. Do not edit by hand.)
[//]: # (To edit docs, change the source comments in the DiscordLuau repo.)

import LuaDeclaration from '$/components/LuaDeclaration.astro';
import LuaProperty from '$/components/LuaProperty.astro';
import LuaSummary from '$/components/LuaSummary.astro';
import LuaParamTable from '$/components/LuaParamTable.astro';

Implementation of the Discord Channel class in Luau. Responsible for implementing the various properties, and methods
for a generic channel object.

<LuaSummary className="Channel" properties={[ { name: "type", type: "string" }, { name: "id", type: "string" }, { name: "name", type: "string?" }, { name: "flags", type: "Classes.Channels.ChannelBitflag?" } ]} methods={[ { name: "deleteChannelAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, Channel<T>>" }, { name: "triggerTypingIndicatorAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, nil>" }, { name: "modifyAsync", isMethod: true, args: [ { name: "json", type: "builders.channelJSON" }, { name: "reason", type: "string?" } ], returnType: "future.FutureLike<string, Channel<T>>" } ]} functions={[ { name: "inheritMethods", args: [ { name: "class", type: "any" } ] } ]} />

## Properties

### type

<LuaProperty name="Channel.type" type="string" />

---
The type of the channel, indicating its functionality and behavior.

### id

<LuaProperty name="Channel.id" type="string" />

---
The unique identifier for this channel.

### name

<LuaProperty name="Channel.name" type="string?" />

---
The name of the channel.

### flags

<LuaProperty name="Channel.flags" type="Classes.Channels.ChannelBitflag?" />

---
The flags that indicate various properties and states of the channel.


## Methods

### deleteChannelAsync

> <LuaDeclaration name="deleteChannelAsync" className="Channel" args={[  ]} returnType="future.FutureLike<string, Channel<T>>" isMethod />

Delete a channel, or close a private message. Requires the MANAGE_CHANNELS permission for the guild, or MANAGE_THREADS
if the channel is a thread. Deleting a category does not delete its child channels; they will have their parent_id
removed and a Channel Update Gateway event will fire for each of them.

<LuaParamTable returns={[ { type: "future.FutureLike<string, Channel<T>>" } ]} />

### triggerTypingIndicatorAsync

> <LuaDeclaration name="triggerTypingIndicatorAsync" className="Channel" args={[  ]} returnType="future.FutureLike<string, nil>" isMethod />

Post a typing indicator for the specified channel, which expires after 10 seconds.

Generally bots should not use this route. However, if a bot is responding to a command and expects the computation to
take a few seconds, this endpoint may be called to let the user know that the bot is processing their message.

<LuaParamTable returns={[ { type: "future.FutureLike<string, nil>" } ]} />

### modifyAsync

> <LuaDeclaration name="modifyAsync" className="Channel" args={[ { name: "json", type: "builders.channelJSON" }, { name: "reason", type: "string?" } ]} returnType="future.FutureLike<string, Channel<T>>" isMethod />

Modifies the channel's settings using the provided JSON data. Optionally accepts an audit log reason.

<LuaParamTable params={[ { name: "json", type: "builders.channelJSON" }, { name: "reason", type: "string?" } ]} returns={[ { type: "future.FutureLike<string, Channel<T>>" } ]} />


## Functions

### inheritMethods

> <LuaDeclaration name="inheritMethods" className="Channel" args={[ { name: "class", type: "any" } ]} />

<LuaParamTable params={[ { name: "class", type: "any" } ]} />


