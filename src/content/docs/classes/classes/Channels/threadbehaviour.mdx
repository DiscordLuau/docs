---
title: ThreadBehaviour
description: DiscordLuau docs for ThreadBehaviour.
sidebar:
  order: 1
---

[//]: # (This file was @generated from moonwave comments. Do not edit by hand.)
[//]: # (To edit docs, change the source comments in the DiscordLuau repo.)

import LuaDeclaration from '$/components/LuaDeclaration.astro';
import LuaProperty from '$/components/LuaProperty.astro';
import LuaSummary from '$/components/LuaSummary.astro';
import LuaParamTable from '$/components/LuaParamTable.astro';

Behaviour class that implements the properties and methods specific to thread channels in Discord.
This behaviour is inherited by thread channel classes to provide thread-specific functionality.

Thread behaviours include:
- Managing thread members (join, leave, add, remove)
- Retrieving thread member information
- Handling thread metadata and statistics
- Managing thread ownership and parent relationships

<LuaSummary className="ThreadBehaviour" properties={[ { name: "totalMessageSent", type: "number" }, { name: "member", type: "Classes.Channels.ThreadMember?" }, { name: "threadMetadata", type: "Classes.Channels.ThreadMetadata?" }, { name: "memberCount", type: "number" }, { name: "messageCount", type: "number" }, { name: "parentId", type: "string?" }, { name: "ownerId", type: "string" } ]} methods={[ { name: "joinAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, nil>" }, { name: "addMemberAsync", isMethod: true, args: [ { name: "userId", type: "string" } ], returnType: "future.FutureLike<string, nil>" }, { name: "leaveAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, nil>" }, { name: "removeMemberAsync", isMethod: true, args: [ { name: "userId", type: "string" } ], returnType: "future.FutureLike<string, nil>" }, { name: "getMemberAsync", isMethod: true, args: [ { name: "userId", type: "string" }, { name: "withMember", type: "boolean?" } ], returnType: "future.FutureLike<string, threadMember.ThreadMember>" }, { name: "listMembersAsync", isMethod: true, args: [ { name: "withMember", type: "boolean?" }, { name: "after", type: "string?" }, { name: "limit", type: "number?" } ], returnType: "future.FutureLike<string, { threadMember.ThreadMember }>" } ]} />

## Properties

### totalMessageSent

<LuaProperty name="ThreadBehaviour.totalMessageSent" type="number" />

---
The total number of messages sent in this thread.

### member

<LuaProperty name="ThreadBehaviour.member" type="Classes.Channels.ThreadMember?" />

---
The current user's thread member object, if they are a member of this thread.

### threadMetadata

<LuaProperty name="ThreadBehaviour.threadMetadata" type="Classes.Channels.ThreadMetadata?" />

---
Metadata about the thread, including archive status and auto-archive duration.

### memberCount

<LuaProperty name="ThreadBehaviour.memberCount" type="number" />

---
The number of members in this thread.

### messageCount

<LuaProperty name="ThreadBehaviour.messageCount" type="number" />

---
The number of messages in this thread.

### parentId

<LuaProperty name="ThreadBehaviour.parentId" type="string?" />

---
The ID of the parent channel this thread belongs to.

### ownerId

<LuaProperty name="ThreadBehaviour.ownerId" type="string" />

---
The ID of the user who created this thread.


## Methods

### joinAsync

> <LuaDeclaration name="joinAsync" className="ThreadBehaviour" args={[  ]} returnType="future.FutureLike<string, nil>" isMethod />

Adds the current user to a thread. Also requires the thread is not archived.

<LuaParamTable returns={[ { type: "future.FutureLike<string, nil>" } ]} />

### addMemberAsync

> <LuaDeclaration name="addMemberAsync" className="ThreadBehaviour" args={[ { name: "userId", type: "string" } ]} returnType="future.FutureLike<string, nil>" isMethod />

Adds another member to a thread. Requires the ability to send messages in the thread. Also requires the thread is not archived.

<LuaParamTable params={[ { name: "userId", type: "string" } ]} returns={[ { type: "future.FutureLike<string, nil>" } ]} />

### leaveAsync

> <LuaDeclaration name="leaveAsync" className="ThreadBehaviour" args={[  ]} returnType="future.FutureLike<string, nil>" isMethod />

Removes the current user from a thread.

<LuaParamTable returns={[ { type: "future.FutureLike<string, nil>" } ]} />

### removeMemberAsync

> <LuaDeclaration name="removeMemberAsync" className="ThreadBehaviour" args={[ { name: "userId", type: "string" } ]} returnType="future.FutureLike<string, nil>" isMethod />

Removes another member from a thread. Requires the ability to manage threads.

<LuaParamTable params={[ { name: "userId", type: "string" } ]} returns={[ { type: "future.FutureLike<string, nil>" } ]} />

### getMemberAsync

> <LuaDeclaration name="getMemberAsync" className="ThreadBehaviour" args={[ { name: "userId", type: "string" }, { name: "withMember", type: "boolean?" } ]} returnType="future.FutureLike<string, threadMember.ThreadMember>" isMethod />

Gets a thread member by their user ID. Can optionally include the member object.

<LuaParamTable params={[ { name: "userId", type: "string" }, { name: "withMember", type: "boolean?" } ]} returns={[ { type: "future.FutureLike<string, threadMember.ThreadMember>" } ]} />

### listMembersAsync

> <LuaDeclaration name="listMembersAsync" className="ThreadBehaviour" args={[ { name: "withMember", type: "boolean?" }, { name: "after", type: "string?" }, { name: "limit", type: "number?" } ]} returnType="future.FutureLike<string, { threadMember.ThreadMember }>" isMethod />

Lists all members in a thread. Can optionally include member objects and use pagination.

<LuaParamTable params={[ { name: "withMember", type: "boolean?" }, { name: "after", type: "string?" }, { name: "limit", type: "number?" } ]} returns={[ { type: "future.FutureLike<string, { threadMember.ThreadMember }>" } ]} />


