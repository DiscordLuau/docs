---
title: Invite
description: DiscordLuau docs for Invite.
sidebar:
  order: 1
---

[//]: # (This file was @generated from moonwave comments. Do not edit by hand.)
[//]: # (To edit docs, change the source comments in the DiscordLuau repo.)

import LuaDeclaration from '$/components/LuaDeclaration.astro';
import LuaProperty from '$/components/LuaProperty.astro';
import LuaSummary from '$/components/LuaSummary.astro';
import LuaParamTable from '$/components/LuaParamTable.astro';

Implementation of the Discord Invite class in Luau

https://discord.com/developers/docs/resources/invite#invite-object

<LuaSummary className="Invite" properties={[ { name: "channel", type: "Classes.Channels.Channel" }, { name: "type", type: "string" }, { name: "code", type: "string" }, { name: "guild", type: "Classes.UnavailableGuild?" }, { name: "inviter", type: "Classes.User?" }, { name: "targetType", type: "string?" }, { name: "targetUser", type: "Classes.User?" }, { name: "approximatePresenceCount", type: "number?" }, { name: "approximateMemberCount", type: "number?" }, { name: "expiresAt", type: "stdPolyfills.DateTime?" }, { name: "scheduledEvent", type: "Classes.Guild.ScheduledEvent?" } ]} methods={[ { name: "getTargetUsersAsync", isMethod: true, args: [  ], returnType: "future.FutureLike<string, { user.User }>" }, { name: "updateTargetUsersAsync", isMethod: true, args: [ { name: "csvContent", type: "string" } ], returnType: "future.FutureLike<string, Invite>" }, { name: "getTargetUsersJobStatusAsync", isMethod: true, args: [  ], returnType: "future.FutureLike< string, apiTypes.GetInviteTargetUsersJobStatusResponse >" }, { name: "deleteAsync", isMethod: true, args: [ { name: "reason", type: "string?" } ], returnType: "future.FutureLike<string, nil>" }, { name: "sync", isMethod: true, args: [ { name: "inviteData", type: "apiTypes.InviteObject" } ] } ]} functions={[ { name: "new", args: [ { name: "state", type: "state.State" }, { name: "inviteData", type: "apiTypes.InviteObject" } ], returnType: "Invite" } ]} />

## Properties

### channel

<LuaProperty name="Invite.channel" type="Classes.Channels.Channel" />

---
The channel this invite is for

### type

<LuaProperty name="Invite.type" type="string" />

---
The type of invite (GUILD, GROUP_DM, FRIEND)

### code

<LuaProperty name="Invite.code" type="string" />

---
The unique invite code

### guild

<LuaProperty name="Invite.guild" type="Classes.UnavailableGuild?" />

---
The guild this invite is for

### inviter

<LuaProperty name="Invite.inviter" type="Classes.User?" />

---
The user who created the invite

### targetType

<LuaProperty name="Invite.targetType" type="string?" />

---
The type of target for this invite (STREAM, EMBEDDED_APPLICATION)

### targetUser

<LuaProperty name="Invite.targetUser" type="Classes.User?" />

---
The user whose stream to display for this invite, if targetType is STREAM

### approximatePresenceCount

<LuaProperty name="Invite.approximatePresenceCount" type="number?" />

---
Approximate count of online members (only present when target_user is set)

### approximateMemberCount

<LuaProperty name="Invite.approximateMemberCount" type="number?" />

---
Approximate count of total members

### expiresAt

<LuaProperty name="Invite.expiresAt" type="stdPolyfills.DateTime?" />

---
The expiration date of this invite

### scheduledEvent

<LuaProperty name="Invite.scheduledEvent" type="Classes.Guild.ScheduledEvent?" />

---
The scheduled event associated with this invite


## Methods

### getTargetUsersAsync

> <LuaDeclaration name="getTargetUsersAsync" className="Invite" args={[  ]} returnType="future.FutureLike<string, { user.User }>" isMethod />

Returns the list of users targeted by this invite's gate.

<LuaParamTable returns={[ { type: "future.FutureLike<string, { user.User }>" } ]} />

### updateTargetUsersAsync

> <LuaDeclaration name="updateTargetUsersAsync" className="Invite" args={[ { name: "csvContent", type: "string" } ]} returnType="future.FutureLike<string, Invite>" isMethod />

Replaces the invite gate's target user list with the provided CSV content.
The CSV should contain one user ID per line with a `user_id` header.

<LuaParamTable params={[ { name: "csvContent", type: "string" } ]} returns={[ { type: "future.FutureLike<string, Invite>" } ]} />

### getTargetUsersJobStatusAsync

> <LuaDeclaration name="getTargetUsersJobStatusAsync" className="Invite" args={[  ]} returnType="future.FutureLike< string, apiTypes.GetInviteTargetUsersJobStatusResponse >" isMethod />

Returns the processing status of the most recent target users upload job.

<LuaParamTable returns={[ { type: "future.FutureLike< string, apiTypes.GetInviteTargetUsersJobStatusResponse >" } ]} />

### deleteAsync

> <LuaDeclaration name="deleteAsync" className="Invite" args={[ { name: "reason", type: "string?" } ]} returnType="future.FutureLike<string, nil>" isMethod />

Delete the attached integration object for the guild. Deletes any associated webhooks and kicks the associated bot
if there is one. Requires the MANAGE_GUILD permission.

<LuaParamTable params={[ { name: "reason", type: "string?" } ]} returns={[ { type: "future.FutureLike<string, nil>" } ]} />

### sync

> <LuaDeclaration name="sync" className="Invite" args={[ { name: "inviteData", type: "apiTypes.InviteObject" } ]} isMethod />

Responsible for syncing data to this invite object. This is typically called by the API when this object is
created or updated.

<LuaParamTable params={[ { name: "inviteData", type: "apiTypes.InviteObject" } ]} />


## Functions

### new

> <LuaDeclaration name="new" className="Invite" args={[ { name: "state", type: "state.State" }, { name: "inviteData", type: "apiTypes.InviteObject" } ]} returnType="Invite" />

Responsible for constructing this class.

<LuaParamTable params={[ { name: "state", type: "state.State" }, { name: "inviteData", type: "apiTypes.InviteObject" } ]} returns={[ { type: "Invite" } ]} />


