---
title: Cache
description: DiscordLuau docs for Cache.
sidebar:
  order: 100
---

[//]: # (This file was @generated from moonwave comments. Do not edit by hand.)
[//]: # (To edit docs, change the source comments in the DiscordLuau repo.)

import LuaDeclaration from '$/components/LuaDeclaration.astro';
import LuaProperty from '$/components/LuaProperty.astro';
import LuaSummary from '$/components/LuaSummary.astro';
import LuaParamTable from '$/components/LuaParamTable.astro';

Class responsible for handling the underlying cache for all objects in Discord-Luau.

Entries are evicted explicitly via `remove()` - callers must call this on DELETE gateway events.
The previous weak-table + TTL-coroutine approach was removed because it could silently evict
entries while they were still valid, and the coroutine workaround that prevented that eviction
made the TTL meaningless.

<LuaSummary className="Cache" properties={[ { name: "store", type: "{ [K]: V }" } ]} methods={[ { name: "set", isMethod: true, args: [ { name: "key", type: "K" }, { name: "value", type: "V" } ] }, { name: "get", isMethod: true, args: [ { name: "key", type: "K" } ], returnType: "V?" }, { name: "remove", isMethod: true, args: [ { name: "key", type: "K" } ] } ]} functions={[ { name: "new", args: [  ], returnType: "Cache<K, V>" } ]} />

## Properties

### store

<LuaProperty name="Cache.store" type="{ [K]: V }" />



## Methods

### set

> <LuaDeclaration name="set" className="Cache" args={[ { name: "key", type: "K" }, { name: "value", type: "V" } ]} isMethod />

Stores a value in the cache under the given key.

<LuaParamTable params={[ { name: "key", type: "K" }, { name: "value", type: "V" } ]} />

### get

> <LuaDeclaration name="get" className="Cache" args={[ { name: "key", type: "K" } ]} returnType="V?" isMethod />

Returns the cached value for the given key, or nil if not present.

<LuaParamTable params={[ { name: "key", type: "K" } ]} returns={[ { type: "V?" } ]} />

### remove

> <LuaDeclaration name="remove" className="Cache" args={[ { name: "key", type: "K" } ]} isMethod />

Removes the cached value for the given key. Call this on DELETE gateway events to keep
the cache consistent with Discord's state.

<LuaParamTable params={[ { name: "key", type: "K" } ]} />


## Functions

### new

> <LuaDeclaration name="new" className="Cache" args={[  ]} returnType="Cache<K, V>" />

Constructor for the Cache class.

<LuaParamTable returns={[ { type: "Cache<K, V>" } ]} />


