[//]: # (This file was automatically compiled through the Generator luau code under this repository)
[//]: # (if you want to edit the documentation of discord-luau, please write changes)
[//]: # (to the discord-luau repository itself, we extract the comments from discord-luau and translate)
[//]: # (them into markdown files that Nextra can interpret!)

[//]: # (----- NEXTRA COMPONENTS IMPORT ----- )

import { Callout } from 'nextra/components'

[//]: # (----- DOCUMENT HEADER ----- )

# DiscordSettings

A simple class to retain some generic data before a Discord Client can be started, for inst;

- The discord bot Token/Authentication
- The discord bot Intents

```lua
	local DiscordLuau = require("../Submodules/DiscordLuau")

	local DiscordIntents = DiscordLuau.DiscordIntents.fromAll()
	local DiscordSettings = DiscordLuau.DiscordSettings.new("BOT TOKEN", DiscordIntents)

	...
```

[//]: # (----- DOCUMENT PROPERTIES ----- )

## Properties

The DiscordSettings instance has no set properties!

[//]: # (----- DOCUMENT METHODS ----- )

## Methods

### setDiscordToken

> DiscordSettings:setDiscordToken( `discordToken` [string](https://www.lua.org/pil/2.4.html) ) ->  DiscordSettings 

(yet-to-do!)

```lua
	
```
### setIntents

> DiscordSettings:setIntents( `discordIntents` [DiscordIntents](/Classes/DiscordIntents) ) ->  DiscordSettings 

(yet-to-do!)

```lua
	
```

[//]: # (----- DOCUMENT FUNCTIONS ----- )

## Functions

### new

> DiscordSettings.new( `discordToken` [string](https://www.lua.org/pil/2.4.html), `discordIntents` [DiscordIntents](/Classes/DiscordIntents) ) ->  DiscordSettings 

(yet-to-do!)

```lua
	
```

