[//]: # (This file was automatically compiled through the Generator luau code under this repository)
[//]: # (if you want to edit the documentation of discord-luau, please write changes)
[//]: # (to the discord-luau repository itself, we extract the comments from discord-luau and translate)
[//]: # (them into markdown files that Nextra can interpret!)

[//]: # (----- NEXTRA COMPONENTS IMPORT ----- )

import { Callout } from 'nextra/components'

[//]: # (----- DOCUMENT HEADER ----- )

# DiscordEmbed

(sorry I will add docs for this lol, just haven't got around to it yet!)

[//]: # (----- DOCUMENT PROPERTIES ----- )

## Properties

### Type

> DiscordEmbed.Type :: \{ ... }

[//]: # (----- DOCUMENT METHODS ----- )

## Methods

### setTitle

> DiscordEmbed:setTitle( `title` [string](https://www.lua.org/pil/2.4.html) ) ->  DiscordEmbed 

(yet-to-do!)

```lua

```
### setType

> DiscordEmbed:setType( `type` [string](https://www.lua.org/pil/2.4.html) ) ->  DiscordEmbed 

(yet-to-do!)

```lua

```
### setDescription

> DiscordEmbed:setDescription( `description` [string](https://www.lua.org/pil/2.4.html) ) ->  DiscordEmbed 

(yet-to-do!)

```lua

```
### setUrl

> DiscordEmbed:setUrl( `url` [string](https://www.lua.org/pil/2.4.html) ) ->  DiscordEmbed 

(yet-to-do!)

```lua

```
### setTimestamp

> DiscordEmbed:setTimestamp( `epoch` [number](https://www.lua.org/pil/2.3.html) ) ->  DiscordEmbed 

(yet-to-do!)

```lua

```
### setColor

> DiscordEmbed:setColor( `colorHex` [number](https://www.lua.org/pil/2.3.html) ) ->  DiscordEmbed 

(yet-to-do!)

```lua

```
### setFooter

> DiscordEmbed:setFooter( `footerSettings` \{ text: string, icon: string } ) ->  DiscordEmbed 

(yet-to-do!)

```lua

```
### setFooter

> DiscordEmbed:setFooter( `imageSettings` \{ imageUrl: string, height: number, width: number } ) ->  DiscordEmbed 

(yet-to-do!)

```lua

```
### setThumbnail

> DiscordEmbed:setThumbnail( `thumbnailSettings` \{ thumbnailUrl: string, height: number, width: number } ) ->  DiscordEmbed 

(yet-to-do!)

```lua

```
### setVideo

> DiscordEmbed:setVideo( `videoSettings` \{ videoUrl: string, height: number, width: number } ) ->  DiscordEmbed 

(yet-to-do!)

```lua

```
### setProvider

> DiscordEmbed:setProvider( `providerSettings` \{ name: string, url: string } ) ->  DiscordEmbed 

(yet-to-do!)

```lua

```
### setAuthor

> DiscordEmbed:setAuthor( `authorSettings` \{ name: string, url: string, iconUrl: string } ) ->  DiscordEmbed 

(yet-to-do!)

```lua

```
### addField

> DiscordEmbed:addField( `fieldSettings` \{ name: string, value: string, footer: string, author: string } ) ->  DiscordEmbed 

(yet-to-do!)

```lua

```
### toJSONObject

> DiscordEmbed:toJSONObject() ->  \{ ... } 

(yet-to-do!)

```lua

```

[//]: # (----- DOCUMENT FUNCTIONS ----- )

## Functions

### new

> DiscordEmbed.new() ->  DiscordEmbed 

(yet-to-do!)

```lua

```

